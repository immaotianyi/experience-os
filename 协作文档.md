# Experience OS 协作文档

更新时间：2026-07-17  
项目路径：`/Users/sanzhaibanniang/Documents/Codex/2026-07-16/gemini-3-1-pro`

## 1. 文档目的

本文用于同步当前 Experience OS 原型进度，并作为“工程 Debug + 升级 AI”与“前端体验与视觉 AI”的共同进入说明。

“工程 Debug + 升级 AI”是专门服务于主开发 AI 的工程搭档。它可以深入检查 bug、架构退化、测试缺口、性能风险、状态机漏洞、Schema 漂移和升级路线，也可以在明确任务范围内直接编写代码、修改文档、补测试并运行验证。它不是只读观察者，而是第二工程执行者；但它仍不能绕过人类方向判断、Human Review 产品语义和主开发 AI 的整体集成责任。

“前端体验与视觉 AI”是专门服务于人类使用者的界面搭档。它将已经稳定的领域状态、API 契约和验证结果，转译为直观、线性、低认知负担的工作台体验。它可以编写前端代码并进行视觉与交互验证，但不重新定义领域模型或替代人类作产品决策。

## 2. 项目核心命题

Experience OS 的中心功能是：

> 将人与 AI 协作过程中的非线性思想，自动转译为可验证、可复用、可审查的经验资产。

当前我们要验证的不是“做一个漂亮工具”，而是验证一套工作哲学能否工程化：

- 协作阶段允许高温发散；
- 生产阶段必须进入低温、结构化、可验证的工程脚手架；
- Skill 不应主要依赖人工查找、安装、调用，而应从真实工作过程中自动生长；
- 每个人的 Skill 都带有生产过程、语言习惯、偏好与上下文依赖；
- 撞墙反馈不只是给 AI 修复代码，也必须反馈给人，让人修正自己的思想模型；
- Human Review 必须适配人的局限：直观、线性、有条理、可判断。

## 3. 当前系统形态

当前是一个零依赖 Node.js ESM 原型，重点是验证领域模型、状态机、Vault、撞墙反馈、自迭代和最小 Web UI。

核心对象包括：

- `Project`
- `ConversationEvent`
- `ThoughtFragment`
- `HumanEditLog`
- `SubgoalSegment`
- `WorkflowPattern`
- `PreferenceHypothesis`
- `Rule`
- `Skill`
- `WallHit`
- `Artifact`
- `ReflectionMemory`
- `MotherSkillTrajectory`
- `ReuseContext`
- `SelfIterationRun`
- `ReviewPacket`
- `ReviewDecision`

这些对象都保存在本地 `work/vaults/` 中，以 JSON 文件形式作为最小 Vault。

## 4. 当前已实现能力

### 4.1 非线性思想到工程对象

`src/pipeline.js` 将对话事件逐步转成：

```text
ConversationEvent
  -> ThoughtFragment
  -> HumanEditLog
  -> SubgoalSegment
  -> Rule
  -> SkillCandidate
  -> WorkflowPattern
  -> PreferenceHypothesis
```

### 4.2 生产验证与撞墙反馈

`src/validate.js` 会检查 Skill 是否满足生产要求，例如：

- 是否有触发意图；
- 是否有触发信号；
- 是否有 `inputSchema`；
- 是否有 `outputSchema`；
- 是否有 fallback；
- 是否声明是否需要人类确认。

如果不满足要求，会生成 `WallHit`，并保存失败原因、阻塞项和建议修复路径。

### 4.3 经验复用

`src/reuseEngine.js` 从 Vault 中检索并生成 `ReuseContext`。检索不是单一全局搜索，而是按资产类型保留配额，避免某一种资产淹没其他资产。

当前复用类型包括：

- `Rule`
- `Skill`
- `ReflectionMemory`
- `WorkflowPattern`
- `PreferenceHypothesis`
- `ThoughtFragment`

### 4.4 Skill 自迭代实验

`src/selfIterationEngine.js` 会基于最近的 `ReuseContext`、`WorkflowPattern`、`ReflectionMemory`、`MotherSkillTrajectory` 自动提出下一批候选 Skill。

当前会生成四类候选：

- 复用上下文构建；
- 撞墙反思沉淀；
- 偏好假设人工确认；
- 自动 Skill 生长编排。

所有自生成 Skill 必须经过生产验证。通过后生成 `Artifact`，失败则生成 `WallHit`。

### 4.5 Human Review

`src/reviewEngine.js` 将偏好假设和自生成 Skill 整理为适合人类线性判断的 `ReviewPacket`。

审查结构固定为：

```text
建议动作
  -> 为什么
  -> 证据
  -> 风险
  -> 可选决策
```

这部分体现了用户提出的关键原则：人类审查必须直观、线性、有逻辑，而不是让人直接面对原始 JSON 或抽象概念。

### 4.6 最小 Web UI

当前已新增最小 Web UI：

- `src/webServer.js`
- `apps/web/index.html`
- `apps/web/styles.css`
- `apps/web/app.js`

运行方式：

```bash
npm run web
```

当前本地访问地址：

```text
http://localhost:4173
```

Web UI 直接读取真实 `work/vaults/` 数据，展示：

- 系统健康状态；
- 中心功能；
- 双漏斗流程；
- 最新 `ReuseContext`；
- 最新 `SelfIterationRun`；
- Human Review 线性审查包；
- WallHit 工程撞墙反馈；
- Skill 列表；
- Vault 资产计数。

当前 Web UI 已支持通过 `POST /api/review-decisions` 提交 Human Review 决策。页面上的审查选项按钮会调用该 API，后端复用 `applyReviewDecision`，并把决策写回目标资产。

当前 Web UI 已支持 Skill / ReviewPacket / WallHit 详情抽屉。详情抽屉分两层：

- 人类可读摘要：状态、目标、触发器、风险、阻塞项、升级门等关键字段；
- 原始 JSON：供工程 Debug + 升级 AI 与工程验证检查字段一致性。

当前 Skill 详情会按需读取 `/api/skill-review-history`，展示该 Skill 的完整审查历史。WallHit 详情会结合 `/api/wallhit-audit` 展示 open/reflected/resolved 状态，并可通过 `POST /api/wallhit-resolutions` 标记为 resolved。

## 5. 当前脚本

```bash
npm run demo
```

生成一个不完整 Skill，用于验证 `WALL_HIT` 路径。

```bash
npm run validate
```

生成一个结构完整 Skill，用于验证 `ARTIFACT_CREATED` 路径。

```bash
npm run reuse
```

从历史 Vault 中构建下一次项目的复用上下文。

```bash
npm run self-iterate
```

让工具基于自身开发过程自动生成下一批 Skill 候选。

```bash
npm run human-review
```

生成适合人类线性阅读的审查包，并应用 demo 决策。

```bash
npm run verify
```

完整验证 demo、validate、reuse、self-iterate、human-review。

```bash
npm run web
```

启动最小 Web UI。

## 6. 重要文档

- `README.md`：当前工程说明；
- `outputs/Experience_OS_0.1_纲领与产品定义.md`：产品哲学、中心功能、纲领；
- `outputs/Experience_OS_0.1_工程蓝图.md`：工程架构、领域对象、落地路线；
- `outputs/Experience_OS_0.1_Web_UI_参考分类与最小实现.md`：Web UI 参考分类与第一版实现策略；
- `outputs/Experience_OS_1.0_验收标准与推进记录.md`：1.0 验收标准、当前进度和剩余缺口；
- `experience-os-2-prd/experience-os-2-prd.html`：Experience OS 2.0 PRD，包含 1.0 就绪度评估、2.0 工程路线、MCP 原生方向、经验资产交易与商业模型；
- `work/devforge_*.md`：从 DevForge 内容整理出的工程参考资料。

## 7. UI 参考分类

当前 Web UI 参考被分为六类：

- `21st.dev`：组件市场与 Agent 检索层；
- `React Bits`：可复制动效组件层；
- `Motion.dev`：状态驱动动效层；
- `GSAP Showcase`：复杂编排与叙事动效层；
- `BentoGrids`：信息密度与布局层；
- `vibe-motion`：Prompt -> Code -> Motion 的工程化脚手架层。

当前实现只吸收信息架构、状态表达和布局原则，不急于引入 React、Motion 或 GSAP。原因是先验证工具核心闭环，再升级前端技术栈。

## 8. 工程 Debug + 升级 AI 的角色边界

该 AI 的核心职责是：

> 专门调试、压力测试和升级主开发 AI 所构建的工程系统，让 Experience OS 不只“能跑”，而是越来越稳定、清晰、可演进。

它不是旁观者，而是工程执行者。它需要指出“为什么不对、怎么复现、怎么修、直接修了什么、修完怎么验证、后续怎么升级”。

该 AI 可以做：

- 阅读代码、文档、Vault 记录、API 返回和命令输出；
- 复现 bug，并给出最小复现步骤；
- 分析失败链路，例如半写状态、字段漂移、状态机断裂、归档断链、UI 误读；
- 设计测试矩阵，包括 happy path、failure path、migration path、rollback path；
- 直接修改代码、文档、测试脚本和本地原型文件；
- 对主开发 AI 的实现提出并落地重构、边界检查和防御性编程；
- 运行不会越权的本地验证命令，例如 `npm run verify`、`npm run validate-vault`、API 冒烟测试和 UI 本地检查；
- 评估升级路线，例如从零依赖 ESM 迁移到 TypeScript、Schema、数据库、React/Vite、正式服务端；
- 发现“现在能跑，但未来会坏”的隐性风险；
- 输出可执行 patch 计划，并在获得当前任务授权时亲自修改文件。

该 AI 不可以做：

- 超出当前任务范围进行大规模重构；
- 未经人类或主开发 AI 明确同意，执行 `npm install`、迁移数据库、清理 Vault、批量归档记录或删除记录；
- 未经说明直接运行高风险写入命令；
- 绕过 Human Review 改变产品语义；
- 替代人类决定架构大方向；
- 单方面替代主开发 AI 做最终集成判断。

工程 Debug + 升级 AI 的建议应优先围绕以下问题展开：

- 当前 bug 是否可复现；
- 失败影响的是理念闭环、工程可靠性、UI 解释层，还是只是局部实现；
- 是否存在字段层、Schema 层、API 层、UI 层的语义漂移；
- 是否存在半写、重复写、不可回放、不可归档、不可恢复的问题；
- 修复是否会污染 Vault 历史记录；
- 修复后用什么命令、API 或 UI 路径验证；
- 这个修复是否应该沉淀成新的 Skill、Rule、ReflectionMemory 或测试脚本。

## 9. 工程 Debug + 升级 AI 的输出格式

工程 Debug + 升级 AI 的建议应尽量使用以下格式：

```text
问题编号：
  Debug-001

观察对象：
  文件 / API / 状态机 / Vault 记录 / UI 流程

复现步骤：
  1. 执行什么
  2. 观察什么
  3. 得到什么错误或异常状态

根因判断：
  字段不一致 / 状态机断裂 / Schema 缺失 / 并发风险 / 归档断链 / UI 误读 / 其他

影响范围：
  影响哪些闭环、对象、API、UI 或验证脚本

修复内容：
  已经改了什么；如果尚未修改，则说明主开发 AI 可以怎么改

验证方式：
  已运行或需要运行哪些命令、API 冒烟或 UI 路径

是否应沉淀为经验资产：
  Skill / Rule / ReflectionMemory / 测试脚本 / 暂不沉淀

优先级：
  P0 / P1 / P2 / P3

是否需要人类判断：
  是 / 否
```

优先级定义：

- `P0`：阻断验证或会导致错误结论；
- `P1`：影响核心闭环或核心概念；
- `P2`：影响可用性、可维护性或一致性；
- `P3`：优化建议、风格建议或未来增强。

## 10. 当前健康状态

最近一次验证结果：

- `npm run verify` 已通过；
- `demo` 能进入 `WALL_HIT`；
- `validate` 能进入 `ARTIFACT_CREATED`；
- `reuse` 能生成 `ReuseContext`；
- `self-iterate` 能生成并验证 4 个候选 Skill；
- `human-review` 能生成线性审查包与 demo 决策；
- Web UI 能读取 `/api/summary`；
- Web UI 能读取 `reviewQueue.pendingReviewCount`；
- Web UI 能读取 `/api/validation` 与 `/api/vault-maintenance`；
- Web UI 能通过 `POST /api/vault-archive` 触发受控归档；
- Web UI 健康状态中：
  - 自迭代为 true；
  - 撞墙反馈为 true；
  - 复用上下文为 true；
  - `humanReviewReady` 表示“是否存在待审事项”，自动 demo 决策完成后通常为 false，配合 `pendingReviewCount: 0` 理解为审查队列为空。

## 11. 当前已暴露并处理的真实问题

开发 Web UI 时曾出现一次字段理解偏差：

`SelfIterationRun` 实际使用 `acceptedSkillIds` / `candidateSkillIds`，但最初 Web UI 健康判断读取了不存在的 `generatedSkillIds`，导致自迭代明明成功却显示为不健康。

这个问题已经在第一轮修正中部分处理，但早期反馈随后发现代码中仍残留 `generatedSkillIds` 死引用。第二轮修正已清理 `src/webServer.js` 与 `apps/web/app.js` 中的死引用，当前运行代码只读取真实字段：

- `acceptedSkillIds`
- `candidateSkillIds`

它是一个有价值的案例：

- 工具观察层必须与真实领域对象保持一致；
- UI 不是装饰层，而是对工程状态的解释层；
- 字段不一致会造成错误的人类判断；
- 这类问题应该沉淀为后续 Schema 校验和 UI 类型约束。

同一轮审核还发现 Human Review 决策只写入 `ReviewDecision`，没有回写目标资产状态。该问题已修正：

- `Skill` 会写入 `status`、`lastReviewDecisionId`、`reviewedAt`、`updatedAt`；
- `PreferenceHypothesis` 会写入 `status`、`confidence`、`lastReviewDecisionId`、`reviewedAt`、`updatedAt`；
- strategic Skill 的默认审查动作已从普通 `approve_candidate` 区分为 `keep_candidate`，语义为“保留候选，不直接升级稳定”。
- 早期反馈随后指出 `keep_candidate` 与 `approve_candidate` 仍然都落为 `candidate_confirmed`，语义没有真正进入工程状态。该问题已继续修正：`keep_candidate` 现在落为 `candidate_retained`，并写入 `promotionGate: "blocked_until_more_evidence"` 与 `candidateReason: "strategic_keep"`。

同一轮继续处理了四个次级问题：

- `lastReviewDecisionId` 与 `reviewedAt` 已进入 `Skill` 和 `PreferenceHypothesis` 的 creator 默认字段；
- `validateSkillForProduction` 与 `validatePreferenceHypothesis` 已允许并检查 review 元字段；
- `applyReviewDecision` 会先准备目标资产回写，再保存 packet / decision，目标缺失时生成 `target_missing` WallHit，避免半写状态；
- `/api/summary` 已新增 `reviewQueue.pendingReviewCount`，`humanReviewReady` 现在表示“是否有待人类处理的审查事项”，不再只是“历史上是否出现过审查包”。
- `/api/validation` 已新增 Vault Schema 校验报告，并区分 supported / unsupported；
- `/api/vault-maintenance` 已新增非破坏性归档预览；
- `/api/vault-archive` 已新增受控归档动作：移动旧记录到 `work/vault-archive/`，生成 manifest，不做删除。

## 12. 下一步建议方向

1.0 原型闭环已完成。2.0-A「工程 Rigor」、2.0-B「MCP 原生与协作」、2.0-C「经验资产交易」及全面安全审核均已交付。下一阶段进入前端迁移与真实管道：

1. 前端从原生 JS 迁移到 React / Next.js：组件化 marketplace 页面（搜索、详情、购买、评分）、质量仪表盘（排行榜、质量报告、低质预警）、卖家收入面板；
2. 接入真实 LLM 跑通思想转译管道：设置 API Key，让 `src/llmAdapter.js` 从 Mock 切换到 OpenAI/Anthropic，验证真实 ConversationEvent → ThoughtFragment → Skill 候选生成；
3. TypeScript 渐进迁移：`domain.js → domain.ts`（Zod Schema 即类型）→ `vault/pipeline → .ts` → `webServer/app → .ts`；
4. 远程 Skill 注册表：从本地索引扩展到远程拉取与同步，支持跨实例 Skill 发现与导入。

## 13. 协作原则

主开发 AI 负责主线实现、集成判断和最终交付。

工程 Debug + 升级 AI 负责复现问题、定位根因、直接修复代码、补测试，并提出升级路线。

前端体验与视觉 AI 负责把稳定的后端状态和人类任务流组织成可见、可理解、可操作的界面，并直接完成前端实现、交互验证与可访问性修复。

人类负责判断方向、审查关键决策、确认偏好和价值取舍。

三者关系应保持清晰：

```text
人类提出非线性思想
  -> 主开发 AI 转成工程对象并实现
  -> 工程 Debug + 升级 AI 复现问题、定位根因、写代码修复或升级
  -> 前端体验与视觉 AI 将已验证状态转译为线性、可判断的界面
  -> 工程验证产生 WallHit / Artifact
  -> 反馈给人类修正思想模型
  -> 进入下一轮协作
```

这个协作模式本身也是 Experience OS 要验证的产品理念。

当主开发 AI 与工程 Debug + 升级 AI 的实现判断冲突时，处理顺序为：

1. 人类判断产品方向和价值取舍；
2. 主开发 AI 判断主线实现路径、工程落点和集成顺序；
3. 工程 Debug + 升级 AI 保留对复现、根因、代码修复、测试和升级路线的执行权；
4. 最终进入工程验证，以 `WallHit / Artifact / validate / verify` 结果校准判断。

## 14. 早期反馈处理记录

时间：2026-07-17

早期反馈发现的问题及处理状态：

- `P1 generatedSkillIds 死引用未清理干净`：已处理。运行代码中已无 `generatedSkillIds` 引用。
- `P1 Human Review 决策未回写目标资产状态`：已处理。`applyReviewDecision` 会更新目标 `Skill` 或 `PreferenceHypothesis`。
- `P2 defaultOption 三元死代码`：已处理。strategic Skill 默认动作改为 `keep_candidate`。
- `P1 keep_candidate 与 approve_candidate 语义坍缩`：已处理。`keep_candidate` 写回 `candidate_retained`、`promotionGate` 和 `candidateReason`。
- `P2 lastReviewDecisionId/reviewedAt 未进入领域模型与校验器`：已处理。
- `P2 applyDecisionToTarget 无容错，目标缺失会导致半写状态`：已处理。目标缺失时生成 `target_missing` WallHit。
- `P2 humanReviewReady 健康信号恒真`：已处理。新增 `pendingReviewCount`，并用待审数量驱动该健康信号。
- `Web UI 真实 Human Review 决策`：已处理。新增 `POST /api/review-decisions`，页面按钮可提交决策并回写目标资产。
- `Web UI 详情视图`：已处理。Skill / ReviewPacket / WallHit 均可打开详情抽屉查看摘要与原始 JSON。
- `Vault Schema 校验与维护预览`：已处理。新增 `npm run validate-vault`、`/api/validation`、`/api/vault-maintenance`，并接入 Web UI Vault 页。
- `核心资产 validator 覆盖`：已处理。当前 Vault 记录 supported 为 100%，invalid 为 0。
- `Web UI 筛选`：已处理。支持 ReviewPacket 状态、WallHit 类型、Skill 层级 / 状态 / promotionGate 筛选。
- `ReviewDecision 审计视图`：已处理。新增 `/api/review-audit` 和 Web UI Audit 页，可追踪决策、目标资产、结果状态与断链情况。
- `自迭代失败路径测试`：已处理。新增 `npm run self-iterate:failure`，并纳入 `npm run verify`。失败候选不会保存为 Skill，而是进入 `WallHit` 与 `SelfIterationRun.rejectedSkillIds`。
- `P2 Vault 无界增长`：已推进第一阶段。新增 preview + archive：`npm run archive-vault`、`npm run archive-vault -- --apply --limit=10`、`POST /api/vault-archive` 和 Web UI 入口。归档只移动文件到 `work/vault-archive/`，并写入 manifest，不删除记录。仍需人类确认长期保留策略和是否允许真正删除。
- `ReuseContext contributionCandidates`：已处理。最新 ReuseContext 会记录每个匹配资产的 `expectedContribution / evidenceScore / outcome`，并在被自迭代使用后回写 `outcome=used_as_self_iteration_context` 与 `usedInRunIds`。
- `Skill review history`：已处理底层 API 与详情抽屉读取。`/api/skill-review-history?skillId=...` 会聚合该 Skill 的 ReviewPacket 与 ReviewDecision。
- `WallHit 修复状态追踪`：已处理第一阶段。WallHit creator 已包含 `status / resolvedByIds / resolvedAt`，`/api/wallhit-audit` 会聚合 ReflectionMemory 与 MotherSkillTrajectory，观察 open/reflected/resolved 状态。`POST /api/wallhit-resolutions` 可把 WallHit 标记为 resolved，并关闭 `humanDecisionNeeded`。
- `P3 slug() 重复定义`：待处理。适合后续抽取公共 util。
- `P3 端口 4173 被占但不响应`：环境问题，按需清理端口。

本轮验证：

- `npm run verify` 通过；
- `rg "generatedSkillIds" src apps package.json` 无运行代码命中；
- 已确认 `Skill` 目标资产被回写为 `candidate_confirmed`；
- 已确认 `PreferenceHypothesis` 在 `defer` 决策后仍记录 `lastReviewDecisionId` 与 `reviewedAt`。
- 已通过 API 冒烟测试确认：pending `ReviewPacket` 可由 `POST /api/review-decisions` 更新为 `decided`，目标资产同步写入 `reviewedAt`。
- 已确认 `npm run verify` 包含 `validate-vault`，当前支持类型 invalid 为 0。
- 已确认 `/api/review-audit` 返回最新 40 条决策审计记录，当前断链数为 0。
- 已确认自迭代失败探针会产生 `WallHit`，`invalidSkillPersisted: false`，且全量 Vault 校验仍通过。
- 已确认 `npm run archive-vault` 能生成归档预览；
- 已确认 CLI `--apply --limit=1` 与 API `POST /api/vault-archive` 均能移动旧记录并生成 manifest；
- 已确认归档后 `npm run validate-vault` 仍为 valid true，invalid 为 0。
- 已确认 `/api/skill-review-history` 能返回指定 Skill 的完整审查包与决策历史；
- 已确认 `/api/wallhit-audit` 能返回 WallHit open/reflected/resolved 状态；
- 已确认最新 `/api/reuse-contexts` 记录包含 `contributionCandidates`。
- 已确认 ReuseContext 被自迭代使用后会回写 `outcome` 与 `usedInRunIds`；
- 已确认 `POST /api/wallhit-resolutions` 能将一条 WallHit 标记为 `resolved`，且 `humanDecisionNeeded=false`；

## 15. 1.0 完成与 2.0 PRD 同步

时间：2026-07-17

1.0 当前状态定义为：

> 原型闭环完成，产品级发布尚未完成。

也就是说，1.0 已完成的是 Experience OS 的核心假设验证：

- 非线性思想可以进入工程对象；
- Skill 可以被生产验证；
- 验证失败会产生 WallHit；
- WallHit 可以沉淀 ReflectionMemory；
- Human Review 可以回写资产状态；
- ReuseContext 可以驱动下一轮自迭代；
- Web UI 可以解释真实 Vault 状态；
- Vault 可以校验、预览归档、受控归档；
- 工程 Debug + 升级 AI 已作为可写代码的第二工程执行者进入协作模型。

1.0 同时完成了视觉温度修复：

- 修复 15+ 处硬编码颜色；
- 亮色与暗色模式统一为暖色系；
- 亮色：底色 `#f6f4ef`，强调 `#c65834`，辅助 `#3d6b54`；
- 暗色：底色 `#1a1714`，强调 `#d97050`，辅助 `#c8a87a`；
- 新增 `--hover-bg / --accent-bg / --rail-bg / --review-bg / --overlay / --error-bg / --empty-bg / --ok-bg / --warn-bg / --mark-bg` 等语义化 CSS 变量；
- 清理 3 个冗余暗色覆盖规则；
- 浏览器验证亮暗模式配色一致。

2.0 PRD 的核心结论：

- 1.0 就绪度为 `7.2/10`：核心闭环均分高，但工程基础仍弱；
- P0 缺口：无 Git、无自动化测试、无真实 LLM；
- 2.0-A：工程 Rigor，交付 Git Vault、测试框架、真实 LLM、TypeScript；
- 2.0-B：MCP 原生，Skill 可自动封装为 MCP Server，并进入 Skill 注册表 / 市场；
- 2.0-C：经验资产交易，质量评级、定价授权、市场抽成；
- 审核修复：路径穿越防护、原子写入、授权密钥加密、防重复扣费、Web API 访问控制、38 项边界测试；
- 产品哲学落地：瑞士军刀 = Skill 可折叠为 MCP Server；人文关怀 = Human Review 保留人类决策权；8-bit = 每个状态变更有可见反馈；
- 商业模式：个人免费，团队版 `¥99/人/月`，市场抽成 `15%`，并保留 `¥9.9` 单次付费切入路径。

## 16. 2.0-A 工程 Rigor 推进

时间：2026-07-17 22:17 - 00:00

2.0-A 的四个 P0 任务全部完成：

### A1 Git 版本控制集成

- 新增 `src/gitVault.js`，包装现有 `Vault`，每次 `save()` 自动 `git add + commit`；
- 提供 `history(recordId)` 查询资产变更历史，`loadAtCommit(recordId, commitHash)` 加载历史版本，`revert(recordId, commitHash)` 回滚到指定版本；
- 提供 `commitAll(message)`，用于归档移动等非 `save()` 写入场景的批量提交；
- `stats()` 返回 Git 仓库状态（enabled / totalCommits / lastCommit / dirty）；
- Vault 目录已初始化为 Git 仓库，初始 commit + 后续自动 commit 均正常工作；
- Web API 新增 `/api/git/history?recordId=` 和 `/api/git/stats` 端点；
- 工程 Debug 复核时发现 `demo / reuse / self-iterate / human-review` CLI 曾直接使用 `Vault`，会绕过 GitVault 自动提交。已修复为使用 `GitVault`；
- `archive-vault / maintain` 归档移动已接入 GitVault 批量提交，避免归档后 Git dirty。

### A2 自动化测试框架

- 使用 Node.js 内置 `node:test`，零依赖；
- 测试文件位于 `tests/` 目录：`domain.test.js`（17 种记录工厂函数）、`validate.test.js`（所有校验器）、`vault.test.js`（存取/列表/搜索/损坏文件）、`pipeline.test.js`（管道端到端含撞墙路径）、`utils.test.js`（slug/latest/unique）、`llmAdapter.test.js`（Mock 适配器/工厂/模板）、`gitVault.test.js`（Git 集成/history/revert）；
- `npm test` 运行全部 103 个测试，0 失败；
- `npm run verify` 现在包含 `npm test` 作为最后一步。

### A3 LLM 适配器

- 新增 `src/llmAdapter.js`，抽象 `BaseLLMAdapter` 基类 + `MockLLMAdapter`（默认，确定性响应）、`OpenAILLMAdapter`（GPT-4o）、`AnthropicLLMAdapter`（Claude Sonnet）三个实现；
- `createLLMAdapter()` 工厂函数根据环境变量自动选择适配器（`LLM_PROVIDER` / `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`），无 key 时回退到 Mock；
- 内置 token 预算控制：`maxTotalTokens` 超限自动暂停并报错；
- 提供 `PROMPT_TEMPLATES` 模板（extractThoughtFragment / segmentSubgoal / generateSkillCandidate / derivePreference）和 `fillTemplate()` 填充工具；
- Web API 新增 `/api/llm/status` 端点。

### A4 TypeScript 迁移第一阶段

- 新增 `src/types.ts`，定义全部 17 种记录的 TypeScript interface + 枚举常量 + LLM/Vault/GitVault 类型；
- 新增 `tsconfig.json`，开启 strict mode，allowJs 兼容渐进迁移；
- 后续阶段：`domain.js → domain.ts`（Zod Schema 即类型）→ `vault/pipeline → .ts` → `webServer/app → .ts`。

### 版本与验证

- `package.json` 版本号升级为 `2.0.0-a`；
- `npm run verify` 全链路通过（demo + validate + reuse + self-iterate + failure + human-review + validate-vault + 103 tests）；
- 测试覆盖率：domain 100%、validate 全规则、vault 含损坏文件、pipeline 含撞墙路径、utils 全函数、llmAdapter 全适配器、gitVault 含 history/revert；
- 修复后再次运行 `npm run verify` 通过，`work/vaults` Git 状态为 clean；
- `npm run archive-vault -- --apply --limit=1` 验证归档移动会生成 `[VaultArchive] move ...` commit，随后 `npm run validate-vault` 仍为 valid true，Git 状态仍为 clean。

## 17. 2.0-B MCP 原生与协作

时间：2026-07-17 22:17 - 00:00

2.0-B 的四个任务全部完成：

### B1 Skill → MCP Server 自动封装

- 新增 `src/mcpExporter.js`，将 stable Skill 一键导出为自包含 MCP Server 目录（server.json + README.md + index.js + package.json）；
- 映射规则：trigger.intent → tool name，inputSchema/outputSchema → MCP tool schema，safetyLevel → 执行策略，humanConfirmationRequired → readOnlyHint；
- 生成 stdio 传输的 JSON-RPC 2.0 服务器（响应 initialize / tools/list / tools/call），支持 SSE 传输模式；
- Web API：`POST /api/mcp/export`（单个 Skill 导出）、`POST /api/mcp/export-all`（批量导出）、`GET /api/mcp/list`（列出已导出的 Server）。

### B2 Skill 注册表与发现

- 新增 `src/skillRegistry.js`，维护所有 stable Skill 的索引，含元数据（reviewCount / approvalRate / usageCount / qualityScore / qualityGrade）；
- `buildLocalIndex()` 扫描 Vault 构建，`searchIndex()` 支持全文搜索 + skillLevel/status 过滤 + quality/usage/recent/name 排序；
- `importSkill()` 从外部导入 Skill 为 candidate（需经审查后升级 stable）；
- `getSkillMetadata()` 返回完整审查历史 + Git 历史 + 统计；
- `listCategories()` 返回所有 tag 及计数；
- Web API：`GET /api/skill-registry`（搜索 + 过滤 + 排序）、`POST /api/skill-registry/import`（导入外部 Skill）、`GET /api/skills/metadata`（详情）。

### D1 多用户 Vault

- 新增 `src/accessControl.js`，三级角色（owner / editor / viewer）+ 三级可见性（private / team / public）；
- 权限检查：`canRead` / `canEdit` / `canDelete` / `canReview` + `filterReadable` 批量过滤；
- 单用户模式（默认）：ownerId 默认 `system`，visibility 默认 `private`，所有操作允许，零侵入；
- 多用户模式：通过 HTTP 头 `x-user-id` / `x-user-role` 传入用户上下文，`contextFromRequest()` 提取。

### D2 团队审查流

- 新增 `src/teamReviewEngine.js`，扩展 ReviewPacket 支持 `assigneeIds` / `votes` / `discussion` 字段；
- `assignReviewers()` 指派审查人，`submitVote()` 提交预投票（approve / reject / abstain，可变更），`addDiscussionComment()` 添加讨论评论（支持 @mention）；
- `checkConfirmationStatus()` 检查是否达到确认阈值（团队默认 2 票，单人默认 1 票，reject 可提前终止）；
- `finalizeTeamReview()` 根据投票结果生成 ReviewDecision 并关闭 packet；
- Web API：`POST /api/team-review/assign`、`POST /api/team-review/vote`、`POST /api/team-review/discuss`、`GET /api/team-review/summary`、`POST /api/team-review/finalize`。

### 版本与验证

- 测试数从 103 增至 170（新增 67 个：mcpExporter 14 个、skillRegistry 16 个、accessControl 20 个、teamReviewEngine 17 个）；
- `npm run verify` 全链路通过：1644 records、0 invalid、0 corrupt、170 tests、0 fail；
- `work/vaults` Git 状态 clean；
- 2.0-A 工程细节收口：CLI 写入脚本（demo / reuse / self-iterate / human-review / archive-vault / maintain）全部切到 GitVault，`GitVault.commitAll(message)` 支持批量提交，归档移动后自动生成 `[VaultArchive] move ...` commit，`work/vaults` Git 状态在 `npm run verify` 后保持 clean。

## 18. 2.0-C 经验资产交易

时间：2026-07-18

2.0-C 的四个任务全部完成：

### C1 质量评级（`src/qualityRating.js`）

- 市场感知质量评分：usage 25% + approval 20% + reviews 20% + market traction 20% + activity 15%，满分 100；
- S/A/B/C/D 五级分级（S≥90 / A≥80 / B≥70 / C≥60 / D<60）；
- `submitRating()` 支持用户评分（1-5 星），同一用户重复评分会替换旧评分；
- `getRatingSummary()` 返回平均分、分布、评论列表；
- `autoFlagLowQuality()` 自动将 D 级 stable Skill 标记为 `needs_revision` + `promotionGate: "quality_below_threshold"`；
- `getQualityLeaderboard()` 返回按分数排序的排行榜；
- Web API：`POST /api/quality/rate`、`GET /api/quality/ratings`、`GET /api/quality/report`、`GET /api/quality/leaderboard`、`POST /api/quality/auto-flag`。

### C2 定价与授权（`src/pricingEngine.js`）

- 三种定价模型：`free`（免费）、`one_time`（¥9.9-¥99 一次性）、`subscription`（¥3-¥19/月）；
- 三种授权类型：`MIT`（开源）、`Commercial`（商业）、`Team`（团队）；
- 试用机制：每个买家最多 3 次试用，每次 1 个输入；
- 平台抽成：15% 平台 / 85% 作者，`calculateCommission()` 精确到分；
- 授权密钥：`generateLicenseKey()` 生成 `EOS-{TYPE}-{HASH}-{HASH}-{RAND}` 格式，`verifyLicenseKey()` 校验格式与类型；
- `checkPurchasable()` 检查 listing 状态 + pricing 合法性；
- `computePurchaseBreakdown()` 返回完整购买明细（gross / commission / net / breakdown）；
- Web API：`POST /api/pricing/validate`、`GET /api/pricing/commission`、`GET /api/pricing/trial`、`GET /api/pricing/breakdown`、`POST /api/pricing/verify-license`。

### C3 市场发布（`src/marketplace.js`）

- `publishSkill()` 将 stable Skill 发布为 `MarketplaceListing`（拒绝非 stable / 重复 active / 非法 pricing）；
- `unpublishSkill()` / `suspendListing()` 下架或暂停；
- `searchMarketplace()` 全文搜索（skill name / summary / trigger intent / signals），支持 license / pricingModel / sellerId 过滤，recent / downloads / rating / revenue / price 排序；
- `listPublishedVersions()` 版本追踪；
- `recordDownload()` 下载计数；
- `syncListingRatings()` 将 SkillRating 聚合到 listing 的 ratingSum / ratingCount；
- `getMarketplaceStats()` 市场总览（listings / downloads / revenue / commission / pricingModels 分布）；
- Web API：`GET /api/marketplace/search`、`POST /api/marketplace/publish`、`POST /api/marketplace/unpublish`、`POST /api/marketplace/suspend`、`GET /api/marketplace/listing`、`GET /api/marketplace/versions`、`POST /api/marketplace/download`、`GET /api/marketplace/stats`。

### C4 交易与收入（`src/transactionLog.js`）

- `processPurchase()` 完整购买流程：检查可购 → 定价 → 抽成 → 签发授权 → 创建 Transaction → 更新 listing revenue/downloads；
- `processTrial()` 试用流程（零金额，计入试用次数）；
- `refundTransaction()` 退款（标记 refunded + 回退 listing revenue）；
- `getTransactionHistory()` 按 buyer / listing / seller 过滤，按时间倒序；
- `getRevenueSummary()` 卖家收入统计（totalRevenue / commission / netRevenue / byType / topSkills）；
- `verifyBuyerLicense()` 验证买家是否持有有效授权（trial 不计）；
- Web API：`POST /api/transaction/purchase`、`POST /api/transaction/trial`、`POST /api/transaction/refund`、`GET /api/transaction/history`、`GET /api/transaction/revenue`、`GET /api/transaction/verify-license`、`GET /api/transaction/get`。

### 领域层扩展

- 新增 3 种记录类型：`MarketplaceListing`、`Transaction`、`SkillRating`（总计 20 种）；
- `domain.js` 新增 5 个常量（PRICING_MODELS / LICENSE_TYPES / LISTING_STATUSES / TRANSACTION_TYPES / TRANSACTION_STATUSES）+ 3 个工厂函数；
- `validate.js` 新增 3 个 validator 并注册到 validateRecord / validateVault；
- `vault.js` 新增 3 个 collection dir；
- `gitVault.js` 新增 3 个 COLLECTION_DIRS + ID 前缀匹配；
- `types.ts` 新增 3 个 interface + 5 个 const enum + Pricing interface + AnyRecord 联合类型扩展。

### 版本与验证

- 测试数从 170 增至 303（2.0-C 新增 106 个单元测试 + 22 个端到端集成测试 + 5 个边界测试）；
- `npm run verify` 全链路通过：1784 records、0 invalid、0 corrupt、303 tests、0 fail；
- Web API 冒烟测试通过：`/api/marketplace/stats`、`/api/marketplace/search`、`/api/quality/leaderboard`、`/api/quality/report`（真实 skill 获得 80 分/A 级）、`/api/pricing/commission?amount=9.9`、`POST /api/marketplace/publish`（正确拒绝非 stable）、`POST /api/pricing/validate`（正确拒绝超范围价格）、`POST /api/transaction/purchase`（正确拒绝不存在的 listing）；
- `work/vaults` Git 状态 clean。

### 自检与修复（2026-07-18）

端到端集成测试 `tests/marketplace-e2e.test.js`（22 个测试）跑通完整市场生命周期：publish → search → download → purchase → trial → rate → quality report → revenue → refund → unpublish → stats。

自检发现并修复 3 个问题：

- **P1 质量报告评分数据源错误**：`getSkillQualityReport` 从 listing 缓存的 `ratingSum/ratingCount` 读取评分，但 `submitRating` 写入的是 SkillRating 记录，两者未自动同步，导致评分提交后质量报告显示 0 评分。修复：改为直接从 SkillRating 记录聚合，保证数据始终准确；同时 listing 过滤从 `active` 扩展为全部（历史下载和收入也应计入质量）；
- **P1 trial 退款语义错误**：`refundTransaction` 允许退 trial 并标记为 refunded，语义错误（trial 是免费试用，不可退款）。修复：新增 `type === "trial"` 检查，直接拒绝；
- **P2 输入校验缺失**：`processPurchase` 不校验 `buyerId/listingId` 是否为空，`submitRating` 不校验 `skillId/userId` 是否为空。修复：新增非空字符串校验。

### 全面安全审核（2026-07-18）

对核心存储层、2.0-C 业务模块和 Web API 层进行全面审核，共修复 6 个 P0 + 11 个 P1 + 4 个 P2 问题，新增 38 项边界测试。

**P0 安全修复：**
- **路径穿越**：`vault.js` 所有 ID 直接拼接文件路径，可被 `../../etc/passwd` 穿越。新增 `validateId()` 白名单（`/^[a-zA-Z0-9._\-]+$/`）+ `..` 检测；
- **授权密钥弱随机**：`pricingEngine.js` 用 `Math.random()` 生成密钥随机段。改用 `crypto.randomBytes(4)`（32 位熵）+ 严格正则校验；
- **确定性 ID 覆盖**：`selfIterationEngine.js` 4 个 Skill ID 固定不变，重跑覆盖已审查 Skill。追加 `Date.now()` 后缀；
- **Web API 无访问控制**：`accessControl` 已实现但未接入 `webServer`。list/search/history 端点接入 `filterReadable`，publish/import/review 接入 `applyOwnership`，review/wallhit 接入 `canEdit`，单用户模式零侵入。

**P1 数据/健壮修复：**
- Review ID 前缀 `review.`/`decision.` 与 `gitVault.findCollectionDir` 不匹配 → 对齐为 `review_packet.`/`review_decision.` + 旧前缀别名；
- `processPurchase` 不检查重复授权 → one_time 购买前检查 `verifyBuyerLicense`；
- `refundTransaction` 不回退 downloads → 新增 downloads 回退；
- `submitRating` 不同步 listing 缓存 → 内部调用 `syncListingRatings`；
- Transaction 无 `updatedAt` → domain 工厂新增字段；
- `submitVote` 允许对已 decided 的 packet 投票 → 新增状态检查；
- Web API 错误泄露 `error.stack` → `safeErrorMessage` 只返回通用消息；
- `readJsonBody` 不防 `null` JSON 体 → 新增类型守卫；
- GET `limit` 参数无上限 → `clampLimit` 限制 500。

**P2 校验修复：**
- `validateRecord` 对 unknown kind 返回空数组（默认通过）→ 改为返回错误；
- Transaction 无 amount/commission/netToSeller 一致性校验 → 新增等式校验（±0.01 容差）；
- Transaction 无 licenseKey 必填校验 → 非 trial 必须有且格式合法；
- Trial 交易金额无约束 → 必须为 0。

**验证结果：** 2026-07-18 复验 `npm run verify` 与 `npx tsc --noEmit` 均通过：342 tests、0 fail、1901 records、0 invalid、0 corrupt、Git clean。

## 19. React 工作台迁移与审查（2026-07-18）

时间：2026-07-18

将原生 JavaScript Web UI 完整迁移为 React 工作台，并完成系统性代码审查与修复。

### React 工作台实现

- 技术栈：React 18 + Vite 5 构建，产物输出到 `apps/web/`，由 `webServer.js` 提供静态文件服务；
- 9 个完整视图：Overview（总览）、Marketplace（市场）、Quality（质量看板）、SellerRevenue（卖家营收）、Review（审查包）、WallHits（撞墙）、Skills（Skill 库）、Audit（决策审计）、Vault（维护）；
- 组件架构：`useFetch` 通用 GET hook + `useToast` 通知队列 + `DetailDrawer` 滑入面板 + `useTheme` 主题切换；
- API 层：5 个 API 模块（core/marketplace/quality/transaction/client）覆盖全部 25+ 端点；
- SPA 路由：webServer 新增 SPA fallback，非静态文件请求回退到 `index.html`；
- 键盘快捷键：1-9 切换视图、R 刷新、Esc 关闭抽屉。

### 审查与修复

**React hooks 误用修复：**
- `MarketplaceView` 错误使用 `useMemo` 执行数据获取副作用（应在 render 期间纯计算）→ 改为 `useEffect` + `useCallback` + `useRef` 避免闭包陷阱；
- `ListingDetail` 中的 `useMemo` 数据获取 → 改为 `useEffect`；
- `QualityView`、`SellerRevenueView` 导入但未使用的 `useMemo` → 清理。

**API client 改进：**
- `getJson` 不解析错误响应体中的 `error` 字段 → 新增 `.json().catch(() => ({}))` + `d.error` 检查，与 `postJson` 对齐。

**Summary API 扩展：**
- `buildSummary` 缺少 2.0-C 新增记录类型（MarketplaceListing/Transaction/SkillRating）→ 补全；
- 新增 `marketplace` 字段（activeListings/totalTransactions/totalRevenue），Overview 视图新增市场统计条。

**未使用导入清理：**
- 6 个视图清理 `useToast` 未使用导入（toast 通过 props 传入）；
- `MarketplaceView` 清理 `fetchQualityReport`/`IconDownload`/`statsLoading`/`onClose` 未使用引用。

### 端到端验证

完整 API 端到端测试通过：
- 购买流程：定价预览 → 授权验证 → 购买 → 授权密钥签发 → 重复购买防护；
- 评分流程：提交评分 → 评分汇总 → listing 评分同步；
- 退款流程：退款 → 状态标记 → 收入回退；
- WallHit 解决：标记已解决 → 状态更新；
- 审查决策：决策提交 → 防重复决策；
- 11 个 API 端点全部冒烟通过。

**最终验证：** 342 tests、0 fail；Vault 1954 records、0 invalid、0 corrupt；React build 成功（187KB gzip 57KB）；Vault Git clean。

## 20. 3.0 可用性与可信自治路线（2026-07-18）

2.0 已验证工程原型的对象、审查、版本化、MCP 与市场能力；3.0 的目标不再是继续堆叠功能，而是让用户在真实项目中自然完成“协作 -> 生产 -> 验证 -> 经验沉淀 -> 恰当复用”的闭环。

共同约束：

- 中心功能仍是**自动经验资产化**，而不是 Skill 商店或泛化 AI 助手；
- 从“顺应懒惰”提升为“降低认知交易成本，同时保留理解、选择与撤回”；
- 非线性思想不承诺无失真转译，采用保留原始来源、证据、替代解释、不确定性和适用边界的“忠实压缩契约”；
- 自动化按风险和可逆性分为 `explore / advise / draft / execute / commit`，不允许自生成资产自行升级权限或稳定状态；
- 经验、能力、权限分离：`Evidence/Recipe` 不等于 `Capability`，能力调用必须受 `ConsentScope`、`CapabilityGrant`、`ExecutionPolicy` 控制；
- 人类界面以线性的 `Experience Receipt` 和升级后的 WallHit 呈现：正在改变什么、为什么、证据与替代方案、影响和回退路径；
- 资产价值以真实项目的结果、基线对照、人工改写和错误复用率衡量，不以下载数、资产数量或模型自评替代。

完整架构、对象、阶段验收、指标、研究来源与近期 10 项执行顺序见：

- [Experience_OS_3.0_可用性与可信自治路线图.md](outputs/Experience_OS_3.0_可用性与可信自治路线图.md)

下一步从阶段 0 开始：先修复交易下载数重复记账、隔离验证数据，再建立 `Project + EvidenceLink + Experience Receipt` 的真实经验闭环。

## 21. 3.0 阶段 0：项目入口落地（2026-07-18）

阶段 0 的三项核心纠偏已进入可运行产品：

- Marketplace 购买流程不再在前端重复调用 `recordDownload`；购买与试用均由服务端恰好计一次下载，回归测试覆盖购买、试用与退款计数；
- `vaultPath.js` 将 demo/verify 默认指向 `work/fixtures/`，Web Server 默认使用 `work/vaults/`；本轮验证确认真实 Vault 保持 1954 条记录，fixture 为 255 条，验证数据不再污染产品指标；
- 新增项目主入口，默认进入“项目”而不是总览。用户可以创建项目、记录 `EvidenceLink`、生成携带不确定性与适用边界的 `ExperienceReceipt`，并在线性时间线中查看来源与沉淀结果。

这不是把“项目”再做成一个管理页面。其交互遵循 3.0 的忠实压缩约束：先有来源，再有总结；总结可保留不确定性与边界；系统默认处于 `advise`，只提出建议而不自行执行。

本轮验证：`npm test` 通过（406 tests）；React 生产构建通过（52 modules，gzip 60.37KB）；浏览器实际加载项目入口，现有真实项目与 Evidence/Receipt 表单、时间线均正常渲染，控制台无错误。

阶段 0 剩余工作聚焦真实性而非新面板：受控真实 LLM Alpha、匿名化评估回归集、首批三项目试用。

## 22. 3.0 可信经验核心（2026-07-19）

本轮根据竞争扫描后的结论，将 EOS 的核心从“记住协作”收紧为“只升级已被验证的经验”。实现内容：

1. **Capture Relay**：新增 `ConversationEvent` 的 `sourceTool`、`sourceRef`、`consented` 字段，以及 `POST /api/relay/events`。连接器只能提交用户明确同意保存的片段，内容仅进入本地项目 Vault，不做屏幕监听或隐式采集。
2. **引用完整性**：Experience Receipt、Decision Receipt、Outcome Record 写入时，系统验证项目存在；每个 EvidenceLink 存在、无重复且属于同一项目；Outcome 指向的 Decision 也必须存在且同属项目。`buildProjectTimeline` 对不存在的项目不再伪造空时间线。
3. **受控 AI 写入**：所有核心写入区分 `origin: human | relay | ai`。人类点击记录是直接表达，不被自治等级阻断；AI 自主写入则调用 `ExecutionPolicy`。因此 `advise` 模式下 AI 无法静默落盘，必须升至 `execute` 或由人保存。
4. **经验升级门槛**：新增 `ExperienceAsset`。只有一条 Receipt 具备证据、关联的 Decision 已人工审查、关联 Outcome 是成功结果且严格引用该 Decision 时，`POST /api/experience-assets` 才会创建 `approved` 资产。
5. **人类线性界面**：项目页增加“捕获协作片段”“人工审查决策”“观察实际结果”“验证与升级”四段。资格不足时展示缺少证据或缺少成功验证等理由；用户不必阅读 Vault JSON 才能理解系统状态。

**验证结果**：新增项目引擎回归测试覆盖跨项目证据、AI 越权、无同意 Relay、合法升级；`npm test` 421 passed；React/Vite production build 成功。

**仍未声称完成的部分**：当前 Relay 是可调用的、工具无关的本地 HTTP 协议与界面入口，尚未提供 Codex/Cursor/Claude Code 的原生安装插件；真实 LLM 也仍需要用户配置 API Key。下一阶段应以一个 Codex/MCP connector 和真实项目试用验证捕获质量，而不是继续扩展面板。

## 24. 跨工具接入层：Bootstrap + Capture Relay MCP（2026-07-19）

本轮将 Relay 从 Web API 扩展为 MCP stdio server，目标是让 Codex、Claude Code、Cursor 等 MCP 客户端通过同一协议进入 EOS，而不是为每个工具复制一套存储逻辑。

- 新增 `src/eosRelayMcp.js` 与 `npm run relay:mcp`：支持 `initialize`、`tools/list`、`tools/call`；stdout 保持 JSON-RPC 纯净，Vault 初始化日志走 stderr；
- 工具严格限定为 `eos_capture_collaboration`、`eos_project_readiness`、`eos_verified_experience`、`eos_project_timeline`。外部 Agent 不能通过 Relay 批准 Review 或 promotion；
- `eos_capture_collaboration` 强制 `consented: true`，不接受隐式采集；
- 新增 `src/eosBootstrap.js` 与 `npm run bootstrap -- <workspace> [name] [goal]`：为现有项目创建 `.eos/README.md`、`project.json`、`mcp.json` 和本地 Git Vault，不移动或扫描业务文件；重复执行保留第一份项目身份；
- 新增 MCP 与 Bootstrap 自动化测试，覆盖初始化、工具发现、未授权捕获拒绝、授权捕获和幂等初始化。

## 23. 3.0 阶段 0 审核与 debug（2026-07-18）

对阶段 0 的代码进行系统性审核，发现并修复 6 个问题：

**P0 严重 bug — 3.0 API 全部 POST 端点被 405 拦截：**
- `webServer.js` 的全局 method 白名单未包含 3.0 新增的 6 个 POST 路径（`/api/projects`、`/api/project`、`/api/evidence`、`/api/experience-receipts`、`/api/decisions`、`/api/outcomes`），导致前端 ProjectView 的所有创建/写入操作返回 405 Method Not Allowed；
- 修复：将 6 个路径加入 POST 白名单。

**P1 数据完整性 — projectEngine 校验缺失：**
- `recordOutcome` 未校验 `outcome` 是否在 `OUTCOME_STATES` 中，无效值会被持久化（因为 `vault.save` 不调用 `validateRecord`）→ 新增 `ensureOutcomeState` 校验；
- `addEvidenceLink` 和 `writeExperienceReceipt` 的 `uncertainty` 未拦截 NaN（`Number("abc")` 返回 NaN，`typeof NaN === "number"` 会通过 `hasNumber`）→ 新增 `ensureUncertainty` 函数，用 `Number.isFinite` 拦截 NaN/Infinity；
- `addEvidenceLink` 未校验 `type` 是否在 `EVIDENCE_TYPES` 中 → 新增 `ensureEvidenceType` 校验；
- `writeExperienceReceipt` 也未校验 `outcome` → 同样接入 `ensureOutcomeState`。

**P1 排序稳定性 — timeline 比较器在相等时返回 -1：**
- `listEvidenceForProject`、`listReceiptsForProject`、`buildProjectTimeline` 三处排序比较器在时间戳相等时返回 -1 而非 0，导致排序不稳定，可能丢失或乱序条目；
- 修复：改用 `b.timestamp.localeCompare(a.timestamp)` 确保稳定排序。

**P2 防御性编程 — updateProject 空字符串绕过校验：**
- `if (updates.status)` 对空字符串 `""` 返回 false，导致空字符串不被校验直接写入；
- 修复：改为 `if (updates.status !== undefined)` 显式检查。

**P2 React hooks — ProjectView loadTimeline 闭包：**
- `loadTimeline` 未用 `useCallback` 包装，`useEffect` 依赖数组只有 `selectedId` 并用 `eslint-disable` 绕过；
- 修复：改用 `useCallback` + `loadTimeline` 作为依赖，消除 eslint-disable。

**回归测试：** 新增 10 个回归测试覆盖上述修复（无效 outcome/NaN uncertainty/Infinity uncertainty/空字符串 status/不稳定排序等场景）。

**最终验证：** 416 tests、0 fail；React 构建 196KB（gzip 60KB）；3.0 API 端到端测试通过（创建项目→添加证据→写 Receipt→记录决策→记录结果→构建时间线，6 步全部成功 + 3 个校验拒绝测试通过）；浏览器 ProjectView 实际渲染正常，控制台无应用错误；两个 Vault 均 Git clean。

## 25. 3.1 Alpha：工作节点入口（2026-07-19）

3.0 的项目页虽然具备真实性，但首次记录要求用户分别理解“协作片段、证据、收据”，认知成本过高，不符合 EOS 的中心功能：在不夺走人类判断的前提下，让经验自然沉淀。

本轮新增 `WorkCheckpoint`，将用户可见动作收敛为一次明确同意的“保存工作节点”：

- 用户填写标题、来源工具、发生了什么与可选说明，并主动勾选本地保存同意；
- 系统一次性创建可回放的 `ConversationEvent`、可引用的 `EvidenceLink` 和人类可读的 `WorkCheckpoint`；
- 三者均属于同一个 Project，Evidence 仍回写到 `Project.evidenceLinkIds`，时间线与 Vault 校验均能看到该节点；
- 未获明确同意时不会写入任何对象；这不是背景监听，也不把“记录”伪装为自动化；
- 原有 Evidence、Receipt、Decision、Outcome 表单暂保留为高级路径。工作节点是自然入口，收据与验证仍是后续的受控漏斗。

这使 3.1 的第一条真实体验链变为：`工作告一段落 -> 保存工作节点 -> 可追溯素材 -> 人工确认的经验收据 -> 生产结果验证 -> 可复用经验`。

## 26. 3.1 Alpha：受控收据草案（2026-07-19）

第二步不把 LLM 直接变成“经验生产者”，而是新增 `ExperienceReceiptDraft`：它是可审阅的提议，不是事实、更不是可复用资产。

- 用户在项目页主动点击“生成草案”；系统最多读取同一项目最近三个 `WorkCheckpoint`，每个节点必须指向已同意保存的 `ConversationEvent` 与同项目 `EvidenceLink`；
- 模型只能依据这些来源输出阶段、总结、结果、不确定性、反例、适用边界与教训；引用的 Evidence ID 由系统而非模型决定，防止模型伪造来源；
- 草案状态为 `pending_review / deferred / accepted / rejected`。未确认或暂缓草案无法进入 ExperienceAsset；
- 人类可直接确认、改写后确认、明确暂缓或驳回；只有确认时才创建正式 `ExperienceReceipt`，其 `sourceDraftId` 反向指向草案；暂缓或驳回只更新草案，不删除原始节点或证据；
- 目前可用 Mock / OpenAI / Anthropic adapter。Mock 只用于离线验收，界面与记录会显示 provider/model，避免把模拟结果冒充真实模型结论。

本轮新增回归测试覆盖跨项目节点拒绝、明确同意、草案到收据的人工确认、驳回不影响原始来源。真实 Web API 也完成验收：`checkpoint -> receipt_draft(pending_review) -> receipt(sourceDraftId)`，Receipt 保留同一 EvidenceLink。

## 27. 验证漏斗：Receipt 与决策一对一（2026-07-19）

为防止同一项目中“某条决策成功”被错误归因给另一条 Receipt，`DecisionReceipt` 新增可选 `receiptId`。当决策审查一条 Receipt 时，工程层验证它属于同一项目，并且决策必须引用 Receipt 的全部 EvidenceLink；升级 ExperienceAsset 时也强制 `decision.receiptId === receiptId`。

`getProjectReadiness` 不再寻找项目中任意成功的人工审查决策，而只接受明确指向当前 Receipt 的成功闭环。UI 的“人工审查决策”自动关联当前最新 Receipt。新增回归测试确保 A Receipt 的成功不能让 B Receipt 获得升级资格。

## 28. 前端体验与视觉 AI：正式进入（2026-07-19）

### 核心职责

> 将 EOS 已被工程验证的状态，翻译为人类无需阅读 Vault、日志或代码也能理解和判断的工作台体验。

它服务的不是“做得更花哨”，而是 EOS 的人类适配原则：协作可以发散，但人类看到的下一步必须线性、明确、可撤回。它需要特别保证用户能区分：什么是原始来源，什么是 AI 草案，什么已被人工确认，什么已被结果验证，什么只是下一次可参考的建议。

### 可以直接做

- 修改 `apps/web-react/` 中的 React 视图、组件、样式、前端 API client 与前端测试；
- 在既有 API 契约上补齐加载、空白、错误、禁用、确认、移动端与键盘可访问状态；
- 审视信息层级、文案、交互路径、视觉一致性和状态反馈，并用浏览器实际验证；
- 为后端稳定字段设计清晰的状态表达，例如来源链、待审草案、验证资格、经验建议与 WallHit；
- 提出需要新增或调整的 API 契约，但不能自行假设后端已经具备该能力。

### 不可以做

- 修改 `src/` 的领域模型、Vault、状态机、验证规则、自治等级或后端写入语义；
- 将 AI 草案伪装为事实，将建议伪装为已验证经验，或隐藏 Human Review 的必要性；
- 通过前端绕过同意、项目归属、证据引用、人工决策或 ExperienceAsset 升级门槛；
- 因视觉偏好而重定义产品中心功能，或制造脱离工作流的营销式首屏。

### 接入顺序与交接协议

本阶段采用串行协作，避免后端状态与前端解释层同时漂移：

1. 工程 Debug + 升级 AI 先审核并修复后端、测试和 API 契约；
2. 主开发 AI 确认接口行为、字段含义、错误语义和验收案例，形成可交接的稳定边界；
3. 前端体验与视觉 AI 仅在该边界内实施界面与交互，提交界面证据、构建结果和需要后端澄清的清单；
4. 主开发 AI 集成并验证完整链路；人类再以真实项目任务评价是否降低了理解和操作成本。

文件所有权默认划分为：工程同事优先负责 `src/`、`tests/` 与后端契约；前端同事优先负责 `apps/web-react/`；主开发 AI 负责跨边界改动、产品文档、冲突协调和最终验收。任何跨边界改动必须先明确写入协作文档或交接说明。

### 前端验收标准

项目主线页面至少要让用户在一次阅读中回答五个问题：

1. 我现在正在做什么项目，下一步最合适的动作是什么？
2. 哪些内容是我明确同意保存的原始来源？
3. 哪一份经验收据仍是草案，是否需要我确认或驳回？
4. 一份收据距离“可升级经验”还差哪一个验证环节？
5. 系统给我的经验建议来自哪里、适用边界是什么、我能否拒绝并留下原因？

每个关键动作必须拥有明确的加载、成功、失败与不可执行状态；页面不能依赖颜色单独表达状态，也不能让用户在同一屏混淆“记录”、“审查”、“升级”三种不可逆程度不同的行为。

## 29. 3.1 Alpha：已验证经验的回流（2026-07-19）

在 Receipt、Decision 与 Outcome 的严格一对一验证之后，项目页新增“下次可用 / 已验证经验建议”。它只从其他项目中读取 `approved` 的 `ExperienceAsset`，同时显示来源项目、收据、适用边界、引用证据和匹配理由；未经验证的草案、候选或本项目内部记录不会被伪装成可复用经验。

用户可对每条建议作出“采纳 / 忽略 / 不适用”的明确反馈，系统将其写入可追踪的 `ReuseContext`，而不是把点击当作无来源的偏好结论。这样 3.1 的最小闭环完整为：

`保存工作节点 -> 受控收据草案 -> 人工确认 -> 决策与实际结果 -> 已验证经验 -> 下一项目中的有边界建议 -> 人类反馈`。

该能力的后端实现已通过既有全量测试与 React 生产构建；下一步由工程同事补充建议筛选、反馈写入和异常目标的专门回归测试，再交给前端同事优化其可理解性与操作节奏。

## 30. TRAE WORK 隔离接入试验（2026-07-19）

为验证 EOS 的跨工具接入能力，本轮为 TRAE WORK 准备独立试验路径，而不让外部编码工具直接接触主产品 Vault：

- 试验目录为 `work/experiments/trae-work-pilot/`，它拥有自己的 `.eos/vault/` 与 Git 历史；
- 主产品仍只使用 `work/vaults/`，demo/verify 仍只使用 `work/fixtures/`；三者不能通过环境变量或软链接混用；
- 试验接入使用 Bootstrap 生成的 `.eos/mcp.json`，由 MCP Relay 暴露有限的捕获与只读工具；它不能替人批准、升级或修改 Vault JSON；
- 为避免同一记录和 Git 自动提交产生并发歧义，一份 Vault 同一阶段只允许一个写入入口。TRAE MCP Relay 写入时，Web 工作台、脚本和其他 Agent 对该 Vault 仅只读；
- 发生时间线缺失、来源不匹配、MCP 失败或疑似双写时，立即停止写入，保留错误给工程 Debug + 升级 AI 复现，不允许手改记录文件。

完整操作与首轮验收标准见 `outputs/TRAE_WORK_EOS_试验接入说明.md`。这次试验的成功定义不是“TRAE 能连上”，而是用户能看懂一条经验从明确同意的来源开始，经过审查与结果验证，最终才成为可复用建议。

首轮隔离试验实际发现并修复了两项接入缺口：其一，中文项目名原先会生成不符合 Vault 文件 ID 白名单的内部 ID，现以 `safeIdSlug` 将内部 ID 编码为 ASCII，同时保留中文显示名称；其二，Relay 原先只写入 `ConversationEvent`，使跨工具记录停在原材料层，现一次获同意的 `eos_capture_collaboration` 会在同一受控操作中连续创建 `ConversationEvent + EvidenceLink + WorkCheckpoint`。MCP 回归测试覆盖拒绝无同意捕获与完整三对象链路；实际 TRAE 试验 Vault 已验证该链路可读。跨记录事务回滚仍是下一轮工程审核项，不能把当前文件级原子写入误称为数据库事务。

TRAE 开始真实协作后，Vault 新增跨进程写锁：同一路径的 `GitVault.save`、回滚与归档提交均需获得 `.eos-write-lock`，默认等待上限为 15 秒；工作节点在锁内重新读取 Project、连续写入三对象并回写 Evidence 列表，因此并发捕获不会丢失另一条 Evidence。锁超时会明确报出“Vault is busy”，调用方必须停止并重试，不能绕过锁写 JSON。并发回归覆盖两个独立 Vault 实例同时保存，以及两个并发工作节点的完整 Evidence 保留。

## 31. 双同事交付后的集成验收（2026-07-19）

工程 Debug + 升级 AI 与前端体验与视觉 AI 的本轮工作已合流验收：

- 工程侧的 Vault 跨进程写锁、工作节点整链路锁、中文 Bootstrap ID 编码与 Relay 三对象捕获已纳入主线；
- 前端侧完成“温暖编辑式工作台”的基座、项目主线、信任等级表达、关键操作二次确认、导航分组、抽屉焦点管理、键盘和移动端适配；
- 领域边界未被前端改写：草案、确认、验证、经验建议仍由真实 API 与状态机决定，前端仅解释和调用既有契约；
- 集成验证结果：`npm test` 449 passed / 0 fail；React `vite build` 成功，产物为 218.90KB（gzip 67.85KB）；运行中的 `http://localhost:4173` 已确认返回新构建资源与正常项目 API；
- 未为更新前端而中断现有 4173 进程，避免影响正在进行的工作。后续任何后端契约修改仍先由工程同事验收，再交前端解释层接入。

## 32. 真实工作区工作台：TRAE 试验进入可见闭环（2026-07-19）

此前 Bootstrap 与 MCP Relay 已能将 TRAE 的协作保存进各自工作区 `.eos/vault/`，但默认 `4173` 工作台只读取主 Vault，用户无法自然进入自己的试验记录。本轮新增 `npm run workbench -- <workspace> [port]`：

- 只接受已经 Bootstrap 的工作区，读取 `.eos/project.json` 并校验 `projectId`；
- 强制 Vault 路径为该工作区的 `.eos/vault/`，拒绝 manifest 指向边界之外的任意目录；
- 默认使用 `4180`，可显式指定合法端口，因此不干扰主工作台 `4173`；
- 使用同一套 React 工作台与 API，但数据边界由启动时的 `EOS_VAULT_DIR` 固定，不提供任意路径切换或跨 Vault 混合视图。

已对 `work/experiments/trae-work-pilot/` 实际启动独立工作台，项目身份为 `project.trae_work_eos_u63a5u5165u8bd5u9a8c`。Bootstrap、Relay、工作台边界测试通过；全量结果为 `npm test` 451 passed / 0 fail，React production build 成功。下一步不再新增对象，而是让 TRAE 的真实工作节点进入 Draft、人工确认、结果记录与复用建议的完整试用闭环。

## 33. 生产漏斗补强：多记录本地事务（2026-07-19）

跨进程写锁解决了“同时写”的问题，但不能解决“写到一半失败”的问题。本轮在 `GitVault` 上新增 `withTransaction`：它复用同一把跨进程锁，在每个文件第一次被修改前保留原始字节；全部操作成功才将相关文件合为一个 Git commit，任何一步失败则恢复原文件或删除新文件，不留下半链路和半提交。

已接入以下多记录业务动作：

- `addEvidenceLink`：Evidence 与 Project 引用一起成功或回退；
- `writeExperienceReceipt`：Receipt 与 Project 引用一起成功或回退；
- `acceptExperienceReceiptDraft`：Receipt、Project 引用、Draft 的 `accepted` 状态一起成功或回退；
- `captureWorkCheckpoint`：ConversationEvent、EvidenceLink、Project 引用、WorkCheckpoint 一起成功或回退。

故障注入回归确认：工作节点最后一步写失败时不保留事件、证据、节点或 Project 引用；草案确认最后一步写失败时不保留 Receipt、不改变 Draft 状态、不写入 Project Receipt 引用。这样“生产阶段的墙”终于具备了可靠的工程含义：要么形成一条可验证链，要么明确失败并回到人类可理解的原状态。

## 34. 真实试用证据：让 EOS 评价自己（2026-07-19）

项目页新增只读的“试用证据”带状区与 `GET /api/project/trial-evidence`。它不产出综合分数，也不把资产数量伪装成价值，而是从已有记录如实计算：

- 草案总数、待处理数、已处理数及已处理草案的中位耗时；
- Receipt 的来源证据覆盖率；
- 成功 Outcome 与已批准 ExperienceAsset 数；
- 已验证经验建议的采纳、忽略、不适用反馈数；
- 尚不足以主张“EOS 产生复用价值”的具体缺口。

最新隔离 TRAE 工作台运行在 `http://localhost:4181`，实际 API 返回显示：已有工作节点，但尚无已处理草案、已验证资产或建议反馈。这不是失败被隐藏，而是系统明确交给人类的下一步验证任务。全量测试结果为 `npm test` 454 passed / 0 fail；React production build 成功。

## 35. Codex 接入检测与可验证安装（2026-07-19）

EOS 现在提供 `npm run codex:preflight -- <workspace>`。它只读地校验工作区 `.eos` 边界，检测本机 Codex CLI 与当前 MCP 登记状态，并输出一条精确绑定该工作区 Vault 的 `codex mcp add` 命令；不会擅自写入 `~/.codex/config.toml`。

实际检测结果：本机存在 `codex-cli 0.145.0-alpha.18`，但全局 Codex 尚未登记 `experience-os`，因此状态为 `ready_to_install`。为验证配置兼容性，已在 `/private/tmp` 的独立 `CODEX_HOME` 中临时登记同一 EOS stdio Relay，Codex 返回 `enabled`，其 command、args、`EOS_VAULT_DIR` 与隔离 TRAE Vault 完全一致。临时验证不影响用户全局配置，也没有写入协作内容。

这使跨工具承诺具备三层证据：Codex CLI 能识别配置、Relay MCP 自动化测试可列工具并拒绝未同意捕获、真实工作区的 Vault 与工作台可读。最后一层“将 EOS 加入用户真实 Codex 配置”仍必须由人类执行前检输出的显式命令后才能宣称完成。

## 31. 前端体验与视觉 AI：入场审查与优化方案（2026-07-19）

前端体验与视觉 AI 已按第 28 节边界入场，完成整体审查（协作文档全 29 节 + `apps/web-react/` 全部 10 视图、样式与 hooks），未触碰 `src/` 任何后端语义。

**审查发现的关键差距（前端范围）：**

- P1 主线未显形：步骤编号只到“第二步”，建议面板插入打断线性叙事；主路径（工作节点）与高级路径（原始 Evidence/Receipt 表单）同权呈现，首屏 6 个表单；
- P1 隐式绑定不可见：审查/结果表单自动锁定“最新 Receipt”但界面不显示目标，验收第 3、4 问无法回答；
- P1 不可逆程度无视觉区分：“保存节点 / 确认收据 / 升级资产”按钮样式趋同，违反第 28 节验收禁令；
- P2 视觉未系统化：Inter 主字体、圆角 6 档、按钮高度 7 档、面板全带投影、eyebrow 大写无字距；
- P2 样式 bug：`OverviewView` 使用未定义的 `var(--bad-bg)`；导航 rail 中“项目”与“总览”图标重复；移动端 rail 横向溢出；时间线 132px 固定列无断点；无 `prefers-reduced-motion`；表单无内联校验。

**已与人类确认的方向：** 「温暖编辑式工作台」——保留暖纸底 DNA，衬线标题（Noto Serif SC）+ 正文（Noto Sans SC）+ 等宽收据编号（IBM Plex Mono），发丝线替代面板投影，烧橙只留给不可逆动作。实施节奏：先基座 tokens + ProjectView 主线，浏览器验证验收 5 问后再推广。

**后端契约核实结果（无需后端改动即可实施）：**

1. `/api/reuse-feedback` 的引擎函数 `recordExperienceReuseFeedback` 已支持 `note` 字段 → “拒绝并留下原因”（验收第 5 问）可纯前端实现；
2. 建议条目已含 `sourceProjectId`，前端可用已加载的项目列表映射名称 → 来源链可展示；
3. readiness 仅含 `receiptId/reasons/decisionReceiptId`，前端用已加载时间线映射收据摘要 → 可去掉原始 ID 标题；
4. 草案列表 API 返回全部状态，前端现仅过滤 `pending_review` → “已丢弃草案留痕”可纯前端实现。

**信任等级视觉语言（实施中）：** L0 原始来源=实线细框+mono「来源」；L1 AI 草案=虚线框+「AI 草案·待你确认」+模型署名；L2 人工确认=实线框+「已由你确认」；L3 已验证=实心徽章+✓。形状、线型、图标、文字四重编码，不依赖颜色单独表达。不可逆性三级按钮：记录=ghost、审查/确认=描边、升级=实心烧橙+二次确认。

**下一步交接：** 期 0（tokens）与期 1（ProjectView 主线）完成后提交构建结果与浏览器截图证据；建议工程同事按第 29 节计划补充建议筛选与反馈写入的回归测试，前端不代为假设。

### 期 0+1 完成交接记录（2026-07-19）

**期 0 基座已交付：**

- `index.html` 引入三套 Google Fonts（Noto Serif SC / Noto Sans SC / IBM Plex Mono）；
- `styles.css` `:root` 新增 `--font-display / --font-body / --font-mono / --r-sm / --r-md` tokens，Inter 全量替换为 `var(--font-body)`；
- 圆角收敛为 2 档（控件 4px / 面板 8px），面板投影移除改为发丝线 `border`；
- eyebrow 改 mono 字体 + letter-spacing；h1/h2/h3 改衬线 display 字体；
- 新增 `:focus-visible` 键盘焦点环、`prefers-reduced-motion` 媒体查询；
- 修复 `OverviewView` 未定义 `var(--bad-bg)` → `className="tag bad"`；`App.jsx` 导航「项目」图标从 `IconOverview` 改为 `IconProject`，`rail-item.active` 加 `aria-current`。

**期 1 ProjectView 主线已交付（`ProjectView.jsx` ~700 行重写）：**

- `TrustTag`（L0 来源/L1 草案/L2 确认/L3 已验证 四重编码：形状+线型+图标+文字）；
- `ConfirmButton`（不可逆动作二次确认，4 秒自动复位）；
- `BindingCard`（显式绑定：审查/结果表单显示目标收据摘要与 shortId）；
- `SuggestionCard`（「忽略/不适用」展开 note textarea，写入 `ReuseContext.note`）；
- `loopSteps` 五步进度条（done/current/todo 三态）+ `nextAction` 六分支下一步横幅；
- 高级路径（原始 Evidence/Receipt 表单）折叠进 `<details class="advanced-path">`；
- readiness 人性化 chips（✓证据 / ✗缺证据 / ✓审查+结果 / ✗缺审查/成功结果）；
- `Promise.allSettled` 替代 `Promise.all`，部分失败不再整页崩；
- 已升级的 receipt 显示「已升级·可复用」tag，不再出现已升级条目仍提示「可升级」的 bug。

**构建与 E2E 证据：**

- `vite build` 通过，产物 214KB / gzip 66KB；
- chrome-devtools MCP 全链路走查：保存工作节点 → 生成 AI 草案（验证虚线框 + 「AI 草案·待你确认」+ 模型署名）→ 接受草案（步骤条 01 done / 02 done / 03 current）→ 提交审查决策（BindingCard 显示目标收据）→ 记录实际结果（BindingCard 显示目标决策）→ 升级为经验资产（二次确认：烧橙 → 「确认升级？再次点击」→ confirming 态）→ 建议反馈写入 note；
- 后端 `ReuseContext` 已确认记录 note 字段：`"note":"E2E：当前项目只是界面复检，暂不需要该经验。"` + `"outcome":"not_applicable"`；
- 移动端 390×844 走查通过（topbar/rail/timeline 均已断点适配）；
- console 0 errors / 0 warnings。

**修复的 bug：**

- `eligibleEntry` 未排除已升级 receipt → 新增 `promotedReceiptIds = useMemo(() => new Set(assets.map(a => a.receiptId)), [assets])` 过滤；
- 移动端 topbar 布局溢出 → flex-wrap 断点适配。

**期 2 即将开始：** 信任等级系统推广到 Review / WallHits / Skills / Audit 四视图。SkillsView 状态映射（candidate→L1 / draft→L1 / stable→L3 / needs_revision→L2 / deprecated→L0）；WallHitsView 用 ConfirmButton 替代 text-button 标记已解决；ReviewView 用 BindingCard 显示审查目标 + rationale 输入框替代硬编码 "web ui decision"；AuditView 区分 AI vs 人工决策并加 TrustTag。

### 期 2 完成交接记录（2026-07-19）

**共享组件提取：**

- 新建 `apps/web-react/src/components/trust.jsx`：`TrustTag` / `ConfirmButton` / `BindingCard` / `shortId` / `truncate` / `KIND_LABELS`；
- `ProjectView.jsx` 已改用 `import { TrustTag, ConfirmButton, BindingCard, shortId, truncate, KIND_LABELS } from "../components/trust.jsx"`，移除本地重复定义，减少约 80 行重复代码。

**四视图重构交付：**

- **SkillsView**：Skill 状态→信任等级映射对齐后端 `domain.js` 枚举（candidate→L1 draft「候选·待验证」/ candidate_retained→L1 draft「保留候选」/ candidate_confirmed→L2 confirmed「已确认候选」/ stable→L3 verified「已验证」/ needs_revision→L2 confirmed「需修订」/ rejected→L0 source「已否决」）；详情抽屉新增 `BindingCard` 显示触发意图 + shortId，以及「最近审查」区显示 `lastReviewDecisionId` 与 `reviewedAt`；
- **WallHitsView**：`text-button` → `ConfirmButton`（二次确认：标记已解决 → 确认解决？再次点击）；新增 rationale 输入「解决说明（可选）」写入后端 `resolutionRationale`；TrustTag 区分 open（L1 draft「待处理」）vs resolved（L2 confirmed「已解决」）；建议修复区加 `binding-label` 标头；
- **ReviewView**：硬编码 `"web ui decision"` → 必填 rationale textarea（空则禁用决策按钮 + 显示 `field-error`）；新增 `BindingCard` 显示审查目标（targetKind + shortId(targetId)）；TrustTag 区分 pending（L1 draft「待决策」）vs decided（L2 confirmed「已决策」）；证据列表用 `wallhit-fixes` 样式；
- **AuditView**：新增「来源」列区分人工 vs 系统（rationale 含 "web ui"/"人工"/"human" → L2 confirmed「人工」，其他 → L0 source「系统」）；理据列不再截断 60 字符，改为完整展示 + `word-break`；新增断链检测（`linked: false` → `audit-broken` 行 + 顶部 `next-action` 预警横幅）；目标类型用 `KIND_LABELS` 中文标签。

**CSS 新增：**

- `.wallhit-fixes`（建议修复区 / 证据列表）、`.wallhit-resolve`（解决区 grid）、`.rationale-input`（rationale textarea + input 共用）、`.rationale-note`（标签 + 输入 + 错误容器）、`.audit-table`（审计表格布局）、`.audit-broken`（断链行 warn-bg）、`.audit-rationale`（理据列 max-width + word-break）；
- 移动端 `.audit-rationale` 断点适配。

**构建与浏览器验证：**

- `vite build` 通过，53 模块，产物 217KB / gzip 67KB；
- SkillsView 截图验证：TrustTag「已确认候选」实心绿、「保留候选」虚线橙、「候选·待验证」虚线橙、「需人工确认」绿描边；
- SkillsView 详情抽屉：BindingCard「触发意图」+ shortId、最近审查区（决策 ID + 时间）；
- WallHitsView 截图验证：TrustTag「已解决」绿色实线 /「待处理」橙色虚线 + 建议修复区；
- ReviewView 截图验证：BindingCard「审查目标」+ TrustTag「已决策」+ rationale 必填输入框 + 决策按钮禁用态；
- AuditView 截图验证：6 列表格（时间/来源/目标/决策/结果/理由）+ TrustTag「系统」标记 + 完整理据展示；
- console 0 errors / 0 warnings。

### 期 3 完成交接记录（2026-07-19）

**导航分组（`App.jsx`）：**

- 原 `VIEWS` 扁平数组重构为 `RAIL_GROUPS`（4 组）：3.0 主线（项目/总览）→ 生产管道（审查包/撞墙/Skill 库）→ 2.0 交易（市场/质量看板/卖家营收）→ 治理（决策审计/Vault）；
- `VIEWS` 由 `RAIL_GROUPS.flatMap(g => g.items)` 派生，键盘快捷键 1-9/0 与 `key` 字段保持映射；
- rail 渲染改为 `RAIL_GROUPS.map`，每组 `.rail-group` 容器 + `.rail-group-label` 垂直文字标签（`writing-mode: vertical-rl`）+ 组间发丝线分隔；
- `<nav>` 加 `aria-label="主导航"`，每个 rail-item 保留 `aria-current` 语义。

**CSS 新增（`styles.css`）：**

- `.rail-group`（flex column 容器）、`.rail-group-label`（mono 字体 + `vertical-rl` + opacity 0.5）、`.rail-group + .rail-group`（border-top 分隔线）；
- 移动端 `@media (max-width: 760px)` 内合并 `.rail-group` 横向排列 + `.rail-group-label` 隐藏规则；
- `.record-card:focus-visible`（accent-2 边框 + accent-bg 阴影环，替代默认 outline）。

**DetailDrawer 焦点管理（`DetailDrawer.jsx` 重写）：**

- 打开时保存 `document.activeElement` 到 `prevFocusRef`，延迟 50ms 聚焦关闭按钮；
- 关闭时从 `prevFocusRef` 恢复焦点到触发元素；
- 焦点陷阱：Tab 在抽屉内最后一个可聚焦元素 → 跳回第一个；Shift+Tab 在第一个 → 跳到最后一个；
- `role="dialog"` + `aria-modal="true"` + `aria-label`（取 `content.title`）；
- Esc 关闭由 `App.jsx` 全局 `keydown` 监听处理。

**键盘可访问性 bug 修复（走查中发现）：**

- `SkillsView.jsx` + `MarketplaceView.jsx` 的 `.record-card` 原为 `<div onClick>` 无 `tabindex`/`role` → 键盘不可达；
- 修复：加 `role="button"` + `tabIndex={0}` + `aria-label`（卡片名称 + 信任标签）+ `onKeyDown`（Enter/Space 触发 `onOpen`）。

**构建与浏览器验证：**

- `vite build` 通过，53 模块，产物 218.81KB / gzip 67.78KB；
- 桌面 1280×800 DOM 验证：`groupCount: 4`、`groupLabels: ["3.0 主线","生产管道","2.0 交易","治理"]`、`railFlexDirection: column`、`labelWritingMode: vertical-rl`、`separatorCount: 3`、`shellMarginLeft: 72px`、`railPosition: fixed`；
- 移动端 DOM 验证：`railFlexDirection: row`、`railPosition: sticky`、`groupLabelDisplay: none`、`shellMarginLeft: 0px`；
- 键盘走查全链路（chrome-devtools MCP 1280×800）：
  - Tab 顺序：项目→总览（3.0 主线）→审查包→撞墙→Skill 库（生产管道）→市场→质量看板→卖家营收（2.0 交易）→决策审计→Vault（治理）→切换主题→刷新→record-card…；
  - SkillsView 聚焦第一张卡片（`role="button"` + `tabindex="0"` + `aria-label="偏好假设人工确认（已确认候选）"`）→ Enter 打开抽屉 → 焦点自动移到关闭按钮（`aria-label="关闭详情"`）；
  - 抽屉内 Tab / Shift+Tab 均不逃逸（`activeInDrawer: true`）；
  - Esc 关闭 → 焦点恢复到触发卡片（`isCard: true`，`activeAriaLabel` 一致）；
- console 0 errors / 0 warnings。

## 36. 严格捕获许可：默认开启（2026-07-19）

用户确认的治理取舍是：外部工具接入默认使用严格许可，而不是把 Agent 传入的 `consented: true` 当作人类许可。原因很直接：AI 可以陈述“已同意”，但不能替人类签发同意。

**可执行链路：**

1. MCP Agent 调用 `eos_prepare_capture_permit`，仅在工作区 `.eos/capture-permits/pending/` 暂存待审内容；它不进入 Vault，也不会被 Git 自动提交。
2. 工作台的项目页显示来源工具、执行者、完整内容与附注。单次内容硬限制为 600 字符，防止人类对截断内容做盲目批准。
3. 人类明确批准后，系统签发五分钟有效、一次性的 `permitId`。许可绑定 `projectId + actor + sourceTool + SHA-256(content)`。
4. Agent 以同一内容、来源与执行者调用 `eos_capture_collaboration`。Relay 先原子认领许可，再写 `ConversationEvent + EvidenceLink + WorkCheckpoint`；第二次使用会失败。
5. 若 Vault 事务写入失败，Relay 仅在许可未过期时恢复为可用，避免“内容没有保存却永久消耗许可”的假失败。

**工程边界：** 待许可、已签发、已消费、已拒绝都留在工作区 `.eos/capture-permits/`，而非经验资产库；只有真正捕获成功的协作片段才进入可追溯的 Vault。严格许可只应用于外部 MCP 捕获。用户在 EOS 工作台亲手填写“保存工作节点”时，仍以该表单中的明确勾选为许可来源。

**验证：** 新增 `capturePermitStore` 测试（完整可见性、内容/来源绑定、并发双花拒绝、长度限制）和 Relay 端到端测试（申请 -> 人类签发 -> 严格捕获 -> 已消费状态）；当前全套测试 459 项通过。

## 37. 严格许可的来源回指（2026-07-26）

严格许可若只作为捕获前的一道门，仍然无法回答一个事后审计问题：项目时间线里的这条协作记录，是否真的来自那次人类批准？本轮把这个缺口补成了来源链的一部分，而非另建一套孤立审计面板。

- `capturePermitId` 成为 `ConversationEvent`、`EvidenceLink` 与 `WorkCheckpoint` 的共同可选字段；
- 严格 MCP Relay 在成功认领许可后，把同一个 `permitId` 传入三对象的受控事务；
- 领域验证器和 TypeScript 接口都声明了该字段，防止 UI 读取字段、领域模型却不承认字段的 Schema 漂移；
- 项目时间线会显示“严格许可 + 短 ID”，让人能区分普通手动保存和经过外部严格许可的捕获；
- 回归测试断言 Relay 的严格捕获响应中，事件、证据、工作节点三者都回指同一许可。

因此现在的链条是：**人类审阅内容 -> 一次性许可 -> 原子捕获三对象 -> 三对象共享许可回指 -> 收据、审查、结果、经验资产**。许可仍不成为经验资产本身；它只是证明经验来源被允许进入系统的治理证据。

## 38. 3.1 Alpha 工程主线验收（2026-07-26）

本阶段完成的是一个可以在本地真实运行、但不虚构“已经验证出市场价值”的 Alpha 核心。它把最初的哲学拆成了两种不同的工程空间：

1. **协作空间可以发散，但进入记录必须有许可。** 外部工具先走严格许可；许可绑定项目、执行者、来源工具与内容哈希，过期后自动成为可见状态。只有捕获成功后，事件、证据和工作节点才共享同一许可回指。
2. **生产空间必须收敛，但结论仍由人作出。** 工作节点只能形成来源受限的 Receipt 草案；人可直接确认、改写后确认、暂缓或丢弃。草案本身永远不能升级为经验资产。
3. **真实模型与离线演练必须分开。** `/api/llm/status` 明确返回 `live / rehearsal`、预算和降级原因。当前支持 OpenAI、DeepSeek（OpenAI-compatible）与 Anthropic；没有真实提供商时，草案 API 返回受控锁定；只有显式设置 `EOS_ALLOW_MOCK_DRAFTS=1` 才可演练，并在记录与界面中标注为非真实模型质量证据。
4. **经验的升级与复用仍受漏斗约束。** Receipt 必须具备同项目来源证据、人工审查、与该审查一对一关联的成功 Outcome，才能成为 approved ExperienceAsset；跨项目建议只读取这类资产，并记录采纳、忽略或不适用反馈及理由。

**本轮循环验证：**

- 许可生命周期与 Relay/项目引擎定向测试通过；
- LLM 状态、草案来源和前端构建定向测试通过；
- 草案的暂缓状态、改写确认契约与领域校验测试通过；
- `npm run verify` 全量通过：462 tests、0 fail；React production build 通过。

**尚未声称完成的事：** 真实 LLM 的质量、真实用户是否节省时间、跨三项目复用是否有效，不能由 Mock、单元测试或我们自己代替。它们是下一步 Alpha 试用需要人类提供的外部证据，而不是工程代码可以伪造的结论。

## 39. 真实 Alpha 证据执行器（2026-07-26）

Alpha 不以“功能已经出现”作为成功标准，而以可反驳的工作证据作为标准。新增 `npm run alpha:evidence -- <workspace>`：它只读 Bootstrap 工作区自己的 `.eos/vault/`，输出 `EOSAlphaEvidenceReport`，明确分开：

- 已同意事件、工作节点与严格许可回指；
- `live` 真实模型草案与 `rehearsal` 离线演练草案；
- 待审、暂缓、确认、驳回四种人类审查状态；
- 成功结果验证、已升级经验和跨项目采纳反馈；
- 仍然缺少的证据，以及是否允许作出价值主张。

它不是打分器：一条 Mock 草案、一次点击或一堆资产都不能让 `valueClaimAllowed` 变为真。真实 Alpha 的最小要求是至少一个真实模型草案、一个被人处理的草案、一个经成功结果验证的资产，以及跨项目采纳反馈。试用中的失败、暂缓和不适用也应当保留，它们是下一轮产品判断的有效证据。

首份可执行的三项目试用流程、隐私边界、人工回收项和阶段退出条件见 [Experience_OS_3.1_Alpha_真实证据执行手册.md](outputs/Experience_OS_3.1_Alpha_真实证据执行手册.md)。TRAE 试验工作区已完成第一次零基线检查：存在 1 个工作节点，但没有真实模型草案、人工处理、成功结果、经验资产或复用反馈；因此 `valueClaimAllowed: false`，这正是 Alpha 开始时应有的诚实状态。

## 40. 常驻注意力状态栏（2026-07-26）

Alpha 阶段新增全局 `GET /api/attention` 与工作台右下角常驻 EOS 三灯。它不是“健康分”，而是为了让正在 Codex、IDE、终端或网页中工作的人，在不打开项目页的情况下知道自己是否需要介入。

- **需要你决定**：红灯只用于等待人类许可的外部捕获；黄灯表示待处理草案或生产审查；绿灯表示当前无待决事项。
- **生产安全**：红灯表示 high/blocker WallHit；黄灯表示仍有可跟进的普通撞墙；绿灯表示无未解决撞墙。
- **模型状态**：绿灯表示真实模型可用；黄灯表示离线演练或模型未配置，不把它伪装成生产故障。

折叠态仅显示 `EOS + 三灯`；展开后才显示三项解释与“下一步”按钮，点击跳到许可、草案、审查或撞墙的对应工作台位置。聚合接口不返回协作原文，只返回计数、状态和导航去向。状态每 15 秒更新一次，也跟随手动刷新更新。

真实 DeepSeek Alpha 请求还发现了模型结构漂移：推理模型可能将数组写为单段文字、将不确定性写为解释、将 outcome 写为自然语言结论。Receipt 草案解析已改为防御性降级：文字列表保留为单项数组；无法证实的 outcome 降为 `unknown`；无法量化的不确定性降为 `null`；每一项校正都会写进 `generationWarnings` 并在工作台展示。模型不合规不会产生半截或伪确定的资产。

## 41. 首份真实模型草案：待人工审查（2026-07-26）

已在隔离的 TRAE WORK 试验工作区中，使用用户授权的节点内容生成首份真实模型草案：`receipt_draft.deepseek_alpha_20260726`。其 `generatedBy` 明确记录为 `deepseek / deepseek-v4-flash / live`，并消耗了实际模型用量；它的来源只链接到该试验项目的单一工作节点与证据。

这不是“验证成功”的宣告。模型将 outcome 与 uncertainty 写成自然语言，EOS 因而将它们分别降级为 `unknown` 与 `null`，并保留五条 `generationWarnings` 供人复核。草案仍为 `pending_review`，没有自动确认、没有生成 Decision/Outcome，也没有升级 ExperienceAsset。

本轮只读 Alpha 报告显示：`live: 1`、`pending_review: 1`、已验证结果 `0`、已批准经验资产 `0`、跨项目采纳 `0`，因此 `valueClaimAllowed: false`。这保住了证据漏斗：真实模型可参与协作发散，但事实性结论与资产升级必须经过人类审查和后续结果。

运行中的临时 workbench 在草案请求完成后未继续对 4189 提供状态 API；这不影响已落盘草案，但使本次无法从该临时进程验证注意力接口是否即时转黄。该问题记录为运行环境待复测项；代码级注意力状态构造与完整回归套件仍已通过。

## 42. macOS 菜单栏入口（Alpha）（2026-07-26）

用户确认第一版优先服务 macOS。注意力三灯不再只作为网页右下角组件，而是新增原生菜单栏入口：`apps/macos/EOSMenuBar/`。它是一个无第三方依赖的 SwiftUI Swift Package，可由 `npm run macos:build` 编译、`npm run macos:run` 启动。

**部署结构：**

1. EOS Core 仍是本机的事实源，默认只监听 `127.0.0.1`，对菜单栏 App 提供聚合后的 `/api/attention`；协作原文不会离开 Core。
2. macOS 菜单栏默认只显示三盏灯，每 15 秒刷新；点击后才展开“需要你决定、生产安全、模型状态”与有限的下一步动作。
3. 工作台仍承接长内容。菜单栏 App 的动作会打开带 `?view=review`、`?view=wallhits` 等深链接的工作台，而不是替人确认或直接写 Vault。
4. App 可在“连接设置”中切换 Core 地址：主 Vault 常用 `127.0.0.1:4173`，隔离项目工作台常用 `127.0.0.1:4180`。这保持了项目间 Vault 的隔离，而不是把所有项目混进一盏状态灯。

**验证：** macOS App 的 Swift 编译通过；React production build 通过；隔离的 Core 在 `127.0.0.1:4190/api/attention` 返回了待审查、撞墙、模型状态三个聚合信号和对应行动。菜单栏入口仍是 Alpha 的未签名本地可执行程序，尚未打包为签名 `.app`、安装器或登录启动项；这些属于下一阶段的分发与常驻策略，不应伪称已经完成。

## 43. macOS 本地 App Bundle 与 Core 生命周期（2026-07-26）

菜单栏入口现已具备可分发的本地 Alpha 包，而不只是 `swift run`：

- `npm run macos:bundle` 编译 release 二进制，生成工作区内的 `dist/EOS.app`；Bundle 采用 `LSUIElement`，因此启动后只驻留菜单栏，不以普通 Dock App 打扰工作；
- `Info.plist` 已通过 `plutil -lint` 校验。它是本机未签名构建，适用于本机 Alpha，不宣称已完成签名、公证或拖拽安装分发；
- `npm run macos:install-core` 是一个**显式**的用户级安装动作：它才会写入 `~/Library/LaunchAgents/local.experienceos.core.plist`，使用当前 Node 绝对路径启动 EOS Core，绑定 `127.0.0.1:4173`，`RunAtLoad + KeepAlive`，日志写入 `~/Library/Logs/ExperienceOS/`；
- `npm run macos:core-status` 只读报告该 LaunchAgent 是否已安装、是否被 `launchctl` 加载；
- 构建 `.app` 不安装后台服务，不触碰系统目录。这是对“可见状态层”和“事实源常驻层”的刻意拆分：人可以先体验三灯，再明确选择是否让 Core 随登录启动。

**验证：** `npm run macos:bundle` release 构建成功，生成 `dist/EOS.app`，且 plist 合法；两个 Core 生命周期脚本通过 Node 语法检查。下一步若用户希望正式常驻，需要由用户明确执行 `npm run macos:install-core`，随后再将 `.app` 移入 Applications、做签名和公证。

## 44. 菜单栏可见性修复（2026-07-26）

首次运行后，进程存在但用户无法看见三灯，说明 SwiftUI `MenuBarExtra` 在该实际 macOS 环境中不能作为“已呈现”的证据。此问题没有被当作用户操作失误，而是替换为更低层的 AppKit 实现：

- `EOSMenuBarApp` 现在通过 `NSStatusBar.system.statusItem` 直接注册系统状态项，并以绘制的 `EOS + 红黄绿三灯` 作为菜单栏图像；
- 点击状态项由 `NSPopover` 承接原有 SwiftUI 详情面板；状态刷新会同步重绘菜单栏三灯；
- 菜单栏代理被显式标记为 `@MainActor`，Swift 6 编译器验证所有 AppKit UI 读写都在主线程；
- 新的 release `.app` 已重新打包并替换旧进程。EOS Core LaunchAgent 已加载，`127.0.0.1:4173/api/attention` 继续返回黄、黄、黄的真实状态。

因此验收标准从“`pgrep` 中存在进程”提升为“macOS Status Bar 中有明确的 EOS 文本和三灯入口”。自动化无障碍截图服务在本环境超时，最终的人类视觉确认仍应优先于进程检测；若仍不可见，下一步就检查菜单栏空间或将入口临时切换为 Dock 窗口，而不是继续假定它已经可用。

## 45. 菜单栏不可见的根因：缺少 App 事件循环（2026-07-26）

用户第二次确认仍不可见后，继续追到真正的启动边界：AppKit 版本只声明了 `NSApplicationDelegate`，却没有调用 `NSApplication.shared.run()`。因此进程可以长期存在，但不会进入 macOS 应用生命周期，`applicationDidFinishLaunching` 不会触发，`NSStatusBar` 项从未注册。此前的进程检测是错误的验收信号。

修复为显式 `EOSMenuBarMain.main()`：创建 `NSApplication`、保留 delegate、设为 accessory activation policy 并调用 `run()`。重新编译、打包、终止旧睡眠进程后，新的 `EOSMenuBar` 进程已启动。此处的工程原则是：**存在性不是可用性；桌面集成必须验证生命周期回调和用户可见的系统表面。**

## 46. 桌面真实交互验收（2026-07-26）

进一步的真实桌面验证暴露了第二个生命周期问题：即使 `applicationDidFinishLaunching` 已执行，`NSApplication` 仍会在没有普通主窗口时自动退出，导致状态项和浮窗随即消失。已通过 `applicationShouldTerminateAfterLastWindowClosed -> false` 让 EOS 作为常驻状态工具保活。

考虑到该 macOS 环境的菜单栏右侧未稳定呈现状态项，Alpha 入口改为双通道：保留 `NSStatusBar`，但启动时同时展示可见的 `EOS 协作状态` 浮窗，并把应用设为普通 Dock App。真实屏幕验收已完成：

1. EOS 浮窗实际显示三条黄灯、下一步按钮、打开工作台和连接设置；
2. 直接点击“1 个生产审查等待决定”；
3. Safari 实际打开 `127.0.0.1:4173/?view=review`，落在“审查包”页面；
4. 网页右下角的 EOS 三灯作为降级入口也同时可见。

这完成了从“环境状态 -> 人类可见摘要 -> 一次点击 -> 对应人工审查面”的真实交互闭环。菜单栏是否在不同 macOS 状态下稳定展示仍是后续兼容性课题，但不再阻断 Alpha 的可见与可用。

## 47. Agent-Hosted 草案通道：默认不需要 EOS API Key（2026-07-26）

用户提出的关键方向被工程化为“借用当前 AI 协作工具的推理”，而不是读取或挪用其云端凭证。新增 MCP 工具 `eos_submit_receipt_draft`：Codex、TRAE 等支持 MCP 的活跃 Agent 可以在自己的当前会话中完成推理，再把结构化候选提交给 EOS。

**严格边界：**

- EOS 不读取 Cookie、登录态、会话 Token 或 API Key；
- EOS 不会主动把 Vault 中的协作原文发给外部云端 Agent；Agent 必须已在自己的正常协作会话中拥有相关上下文；
- 提交只能引用同项目的、已经获得人类同意的 checkpoint / event / evidence 来源链；跨项目引用直接拒绝；
- 写入结果固定为 `generatedBy.mode = agent_hosted` 与 `pending_review`。不论 Agent 如何表述，都不能直接创建 ExperienceReceipt、Decision、Outcome 或 ExperienceAsset；
- 不受控 outcome、uncertainty、数组结构继续走同一套降级与警告机制。

这让 EOS 有三种明确运行模式：**Agent-hosted（默认，免 EOS Key）**、Direct API（用于后台自主生成）和 Local Model（用于离线/隐私场景）。前者把 EOS 放回应有位置：它是跨工具的经验资产化与验证层，而不是另一个要求用户重复登录、重复付费的聊天入口。

**验证：** 新增领域回归：Agent-hosted 草案能在不调用 EOS 模型的情况下保存，但仍必须等待人工确认；跨项目 checkpoint 被拒绝。新增 MCP 端到端回归：Agent 可提交草案、草案带 `agent_hosted` 标记、持久化后仍为 `pending_review`。定向测试 65 项通过，React build 通过。使用契约见 [Experience_OS_3.2_Agent_Hosted_草案协议.md](outputs/Experience_OS_3.2_Agent_Hosted_草案协议.md)。

## 48. Codex 真实 MCP 试验与证据口径修正（2026-07-26）

本轮在隔离工作区 `work/experiments/codex-agent-hosted-pilot/` 完成了真实 Codex 接入试验。Codex 全局 MCP 已注册为 `experience-os`，其进程只获得该试验 Vault 的本地路径与严格许可策略；没有读取 EOS API Key、Cookie、会话 Token 或其他应用内容。

实际落盘的 `codex_real_agent_hosted_draft_20260726` 证明了完整的 Agent-hosted 写入边界：它只引用用户明确授权的无敏感 checkpoint，记录 `provider: OpenAI`、`model: Codex`、`mode: agent_hosted`，并固定停在 `pending_review`。没有自动 Decision、Outcome 或 ExperienceAsset。

试验同时发现 Codex CLI 的输出通道在本机 `chatgpt.com` MCP 初始化连接关闭后不能可靠地回传工具结果；但 Vault 中的实际记录显示工具调用已经完成。这被视为客户端/网络运行环境问题，而非将失败静默归因给 EOS。

因此 Alpha 报告的草案证据改为三类：`live`（EOS Direct API）、`agentHosted`（当前外部 Agent 会话）和 `rehearsal`。前两者共同构成“真实 AI 草案”，但报告不会把它们混为一谈。该修正避免了一个与产品哲学相悖的误报：默认免 EOS Key 的 Agent-hosted 模式不应被统计成离线演练。

## 49. Codex 工作区绑定是可见状态，而非隐式假设（2026-07-26）

Codex 当前版本只提供全局 MCP 配置，不提供按项目自动切换的 MCP Vault。因此 EOS 不把“已注册 experience-os”误说成“当前项目已接入”。`npm run codex:preflight -- <workspace>` 现明确返回三态：`active_for_workspace`、`registered_for_other_workspace`、`ready_to_install`，并显示实际绑定的 `boundVaultDir`。

当 Codex 已绑定另一工作区时，EOS 只生成可审阅的 `codex mcp remove ... && codex mcp add ...` 切换命令，不在预检过程中改写用户的全局配置。这样“当前协作发生在哪个经验边界内”成为人可见、可验证、可主动决定的事实，而不是系统悄悄做出的路由判断。

## 50. Codex 已激活到 TRAE 试验工作区（2026-07-26）

用户确认开始接入后，Codex 的全局 `experience-os` MCP 已从隔离的 Codex 验证 Vault 切换到 `work/experiments/trae-work-pilot/.eos/vault`。切换后预检返回 `active_for_workspace`，`boundVaultDir` 与 TRAE 工作区 Vault 完全一致，严格许可策略保持为 `strict_permit`。

随后只读 Alpha 证据报告确认：工作区仍只有既有的 2 条明确同意事件、1 个工作节点与 1 份 Direct API 真实草案；没有因配置变更新增捕获、草案、决策或资产。当前下一个人类环节仍是处理该 `pending_review` 草案，之后才可记录结果并验证经验升级链。

## 51. 产品重新定义与竞品核对（2026-07-26）

在真实草案进入人工审查前，用户提出了根本问题：“EOS 到底是什么，是否已经被替代？”本轮调研结论是：EOS 不应自称 AI 编程客户端、通用 Agent Memory、LLM 观测平台或 MCP 本身；这些层已经分别由 Codex/Claude Code/Cursor/Copilot、Mem0/Letta/OpenMemory、Langfuse 及 MCP 生态覆盖。

EOS 应守住的中心功能是 **Experience Receipt（经验收据）闭环**：将用户明确同意的协作片段转成有来源的候选主张，经人确认并由后续实际结果验证后，才成为可复用经验。它的价值不在于“记得更多”，而在于“知道哪些经验被证实、适用于何处、还没有证明什么”。

当前最重要的取舍是暂停把 Marketplace、支付和大而全的自动 Skill 作为主线，转而在真实项目中证明一次完整链条：确认收据 -> 成功结果 -> 已验证经验 -> 下一任务采纳 -> 人确认减少了重复判断。完整研究、竞品矩阵、哲学缺口与两周试验方法见 [Experience_OS_产品定义与竞品调研_2026-07-26.md](outputs/Experience_OS_产品定义与竞品调研_2026-07-26.md)。

## 52. 复用试验：把“采纳”变成可验证的价值（2026-07-26）

“用户点击采纳”只能证明界面被操作，不能证明经验有价值。为验证 EOS 最核心的产品主张，系统新增 `ExperienceReuseTrial`：它将一条已批准的跨项目 `ExperienceAsset` 绑定到一个具体的新任务，再由人类记录实际结果与“是否减少了重复判断”。

**边界与状态：**

- 只能使用来自另一项目、且已经批准的经验资产；同项目自我引用会被拒绝，避免把复用伪装成验证；
- 发起时记录目标任务与采用说明，状态为 `adopted`；
- 完成时只能填写 `success`、`partial` 或 `failure`，并由人明确回答是否减少了重复判断；
- 只有 `success` 或 `partial` 且 `reducedRepeatedDecision = true` 的完成试验，才允许 Alpha 报告宣称 EOS 已出现真实复用价值。

工作台中，经验建议的动作从“采纳”改为“用于本次任务”。用户写下任务标题后，界面会生成一张待完成的复用试验卡；完成卡片才出现结果与减少判断的确认项。这样信息从简到繁：平时只看建议，需要时才展开为有责任归属的结果记录。

**验证：** 新增领域、Vault、校验器、HTTP API 与 React 工作台的连接；定向测试与全量 `npm run verify` 均通过。该机制不自动宣称收益，也不替代人的判断，它只把“经验确实帮到了下一次工作”变成一个可被审阅的事实。

## 53. 外部体验 Beta 的部署边界（2026-07-27）

用户决定让更多人体验 EOS。部署不等于把当前本地工作台直接暴露到公网：现阶段的 GitVault 是单 Vault、单写入者设计，公开共享会同时破坏隐私、来源链和结果归属。

本轮已增加 Docker 构建、Render 单实例 Blueprint、`/api/health` 健康检查、生产响应安全头与 `npm run deploy:preflight`。运行时 Vault 必须挂载到持久化绝对路径，容器内保留 Git 以维持资产版本历史。

同时新增 `BetaFeedback`：用户必须明确同意后才能提交产品体验报告，记录可理解性、有效性、继续使用意愿及可选文本；不收集协作原文，不自动影响偏好或 Skill，并有字段长度限制及每地址速率限制。

产品策略明确分为两条：公开展示只用合成数据且只读；真实 Beta 仅邀请少量测试者在各自独立工作区运行，原始协作默认留在本机，中心端只接收明确同意的最小反馈。账号、租户隔离、删除权、备份恢复和反馈面板是进入更广泛云端 Beta 前的主线缺口。完整方案见 [Experience_OS_3.3_外部体验Beta部署方案.md](outputs/Experience_OS_3.3_外部体验Beta部署方案.md)。
